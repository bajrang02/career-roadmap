// ─────────────────────────────────────────────────────────────────────────────
// Curated knowledge for recurring SHELL / LINUX / SYSTEMD / NETWORK / GIT /
// DEPLOY labels injected by the DevOps, Linux, Network and Git skeletons.
// Content is career-invariant: the entry that matches the label applies
// everywhere. Merged into ALL_KNOWLEDGE in generate.mjs (KNOWLEDGE wins).
// ─────────────────────────────────────────────────────────────────────────────

const res = (t, u, k) => ({ t, u, k });

export const META_SHELL = {
  // ── Command line basics ───────────────────────────────────────────────────
  "basic-commands": {
    d: "Basic commands are the everyday tools of the command line — ls, cd, pwd, mkdir, rm, cp, cat, less and echo — the ones you type hundreds of times a day. They are the smallest unit of shell fluency and the foundation every other skill builds on.",
    why: "You cannot operate a server, container, CI job or Linux machine without them; every other shell skill is a variation on these.",
    obj: ["Navigate the filesystem with cd, pwd, ls", "Inspect files with cat, less, head, tail", "Create and remove with mkdir, touch, rm, cp, mv", "Understand command structure: command, flags, arguments"],
    prereq: ["What a shell is", "A terminal emulator"],
    res: [
      res("Linux command line basics — Ubuntu", "https://ubuntu.com/tutorials/command-line-for-beginners", "tutorial"),
      res("Linux commands — GeeksforGeeks", "https://www.geeksforgeeks.org/linux-commands-cheat-sheet/", "reference"),
      res("ExplainShell — what each command does", "https://explainshell.com/", "reference"),
    ],
    int: ["What does ls -la show that ls alone doesn't?", "What's the difference between rm and rmdir?", "When would you use cat versus less?"],
    tips: ["Use tab completion before typing full paths"],
    diff: "Beginner", time: "2–3 hours",
  },
  "moving-files-directories": {
    d: "Moving files and directories is the mv and cp workflow of the shell — relocating, renaming and copying entries within and across filesystems. It is the everyday file-management layer every professional uses constantly.",
    why: "Organised projects, staged deployments and tidy servers all come down to moving the right files to the right place safely.",
    obj: ["Move with mv and copy with cp", "Rename files and directories", "Preserve metadata with -p and recurse with -r", "Move across filesystems and avoid overwriting with -i and -n"],
    prereq: ["Basic commands (ls, pwd, cd)", "File paths"],
    res: [
      res("mv command — Linuxize", "https://linuxize.com/post/linux-mv-command/", "tutorial"),
      res("cp command — Linuxize", "https://linuxize.com/post/cp-command-in-linux/", "tutorial"),
      res("Copy and move — GNU coreutils manual", "https://www.gnu.org/software/coreutils/manual/coreutils.html#cp-invocation", "docs"),
    ],
    int: ["How do you move a directory and everything in it?", "What happens when mv moves a file to an existing name?", "How do you copy while preserving timestamps?"],
    tips: ["Use mv -i when batch-moving to avoid silent overwrites"],
    diff: "Beginner", time: "1–2 hours",
  },
  "creating-deleting-files-dirs": {
    d: "Creating and deleting files and directories is the mkdir, touch, rm, rmdir and truncate workflow — how you build and tear down the structure of a filesystem. It covers safe creation and, critically, safe deletion.",
    why: "Everything you build starts with creating files; everything you clean up ends with deleting them — and rm mistakes are the classic production horror story.",
    obj: ["Create files with touch and redirects", "Create directories with mkdir -p", "Delete with rm, rmdir and their recursive flags", "Use -i, -f and trash-style habits safely"],
    prereq: ["Basic commands", "File paths"],
    res: [
      res("mkdir and touch — Linuxize", "https://linuxize.com/post/how-to-create-directories-in-linux-with-the-mkdir-command/", "tutorial"),
      res("rm command — Linuxize", "https://linuxize.com/post/rm-command-in-linux/", "tutorial"),
      res("Filesystem hierarchy — tldp", "https://tldp.org/LDP/Linux-Filesystem-Hierarchy/html/", "docs"),
    ],
    int: ["What does mkdir -p do?", "Why is rm -rf dangerous and how do you guard against it?", "What's the difference between touch and echo >?"],
    tips: ["Alias rm to rm -i in interactive shells"],
    diff: "Beginner", time: "1–2 hours",
  },
  "command-help": {
    d: "Command help is the built-in documentation every command carries: man pages, --help flags, info pages and the help command. It is the skill of reading the manual instead of guessing.",
    why: "Professionals don't memorise every flag — they know how to look up the right one in seconds, which is far more valuable.",
    obj: ["Read man pages with man and man -k", "Use --help and -h flags", "Search the manual with apropos and grep", "Understand SYNOPSIS, OPTIONS and EXAMPLES sections"],
    prereq: ["Basic commands"],
    res: [
      res("man pages guide — Linux Journey", "https://linuxjourney.com/lesson/man-pages", "tutorial"),
      res("How to use man pages — Opensource.com", "https://opensource.com/article/21/8/linux-man-pages", "article"),
      res("tldr — simplified man pages", "https://tldr.sh/", "docs"),
    ],
    int: ["What's the difference between man and --help?", "How do you search all man pages for a keyword?", "What do the brackets in a SYNOPSIS mean?"],
    tips: ["Use tldr for quick reminders, man for the full picture"],
    diff: "Beginner", time: "1 hour",
  },
  "command-path": {
    d: "The command path is how the shell finds executables: the PATH environment variable lists the directories searched, in order, when you type a command name. It is the mechanism behind 'command not found'.",
    why: "Understanding PATH fixes the most common beginner error in shell setup and lets you control which version of a tool runs.",
    obj: ["Explain what PATH is and how lookup works", "Inspect and edit PATH", "Add directories with export and profile files", "Diagnose 'command not found'"],
    prereq: ["Basic commands", "Environment variables"],
    res: [
      res("PATH explained — Opensource.com", "https://opensource.com/article/17/6/set-path-linux", "article"),
      res("Environment variables — Ubuntu docs", "https://help.ubuntu.com/community/EnvironmentVariables", "docs"),
      res("How PATH works — Linux Handbook", "https://linuxhandbook.com/path-variable/", "tutorial"),
    ],
    int: ["Why does 'command not found' happen even though the program is installed?", "How do you make a PATH change permanent?", "What happens if PATH has two versions of the same tool?"],
    tips: ["Prefix a custom bin dir to override system tools deliberately"],
    diff: "Beginner", time: "1 hour",
  },
  "redirects": {
    d: "Redirects are how the shell routes input and output: > sends stdout to a file, >> appends, < reads stdin from a file, and 2> captures stderr. They are the plumbing that connects commands together.",
    why: "Redirects turn command output into files, logs and pipelines — the basis of every shell script and every log file you've ever read.",
    obj: ["Redirect stdout with > and >>", "Redirect stderr with 2> and 2>&1", "Read input with <", "Understand /dev/null and /dev/stdin"],
    prereq: ["Basic commands", "stdin/stdout/stderr"],
    res: [
      res("I/O redirection — tldp guide", "https://tldp.org/LDP/abs/html/io-redirection.html", "docs"),
      res("Redirections — Bash manual", "https://www.gnu.org/software/bash/manual/bash.html#Redirections", "docs"),
      res("How redirection works — Opensource.com", "https://opensource.com/article/18/8/how-redirection-works-bash", "article"),
    ],
    int: ["What's the difference between > and >>?", "How do you send both stdout and stderr to the same file?", "Why send output to /dev/null?"],
    tips: ["2>&1 must come after the file redirect to combine streams"],
    diff: "Beginner", time: "1–2 hours",
  },
  "shebang-and-execution": {
    d: "The shebang is the first line of a script — #!/bin/bash or #!/usr/bin/env python3 — that tells the kernel which interpreter to run. Execution is the chmod +x and ./script workflow that makes scripts runnable.",
    why: "Shebangs and executable bits are what turn a text file of commands into a reusable tool, and the env pattern makes scripts portable.",
    obj: ["Write a shebang line correctly", "Make scripts executable with chmod +x", "Run scripts with ./ and absolute paths", "Use /usr/bin/env for portability"],
    prereq: ["File permissions", "Writing a simple script"],
    res: [
      res("Shebang explained — Linux Handbook", "https://linuxhandbook.com/shebang/", "tutorial"),
      res("chmod basics — GeeksforGeeks", "https://www.geeksforgeeks.org/chmod-command-linux/", "reference"),
      res("#!/usr/bin/env — Stack Overflow", "https://stackoverflow.com/questions/2429511/why-do-people-write-usr-bin-env-python-on-the-first-line-of-a-python-script", "community"),
    ],
    int: ["Why does a script fail with 'permission denied'?", "What does #!/usr/bin/env python3 solve?", "When is a shebang required?"],
    tips: ["Prefer /usr/bin/env for interpreted languages"],
    diff: "Beginner", time: "1 hour",
  },
  "variables-and-quoting": {
    d: "Variables and quoting are how shell scripts hold and protect data: VAR=value assignment, $VAR expansion, and the quote rules (single, double, backtick, $()) that control when expansion happens.",
    why: "Half of all shell-script bugs come from unquoted variables and misunderstood expansion — mastering quoting is what makes scripts safe.",
    obj: ["Assign and expand variables", "Quote with single and double quotes", "Use command substitution $()", "Understand word splitting and globbing"],
    prereq: ["Basic commands", "Redirects"],
    res: [
      res("Variables — Bash guide (tldp)", "https://tldp.org/LDP/abs/html/variables.html", "docs"),
      res("Quoting — Bash manual", "https://www.gnu.org/software/bash/manual/bash.html#Quoting", "docs"),
      res("ShellCheck — catch quoting bugs", "https://www.shellcheck.net/", "practice"),
    ],
    int: ["What's the difference between single and double quotes?", "Why should variables always be quoted in scripts?", "What does $() do that backticks also do?"],
    tips: ["Always quote variables: \"$var\" — never bare $var"],
    diff: "Intermediate", time: "2 hours",
  },
  "exit-codes-and-error-handling": {
    d: "Exit codes are the number a command returns when it finishes — 0 for success, non-zero for failure — and error handling is how scripts check and react to them with if, && and set -e.",
    why: "Robust scripts must know when a step failed; exit codes are the only reliable signal, and set -euo pipefail is the professional default.",
    obj: ["Read $? and check exit codes", "Chain with && and ||", "Handle errors with if statements", "Write robust scripts with set -euo pipefail"],
    prereq: ["Variables and quoting", "Conditionals"],
    res: [
      res("Exit codes — Bash Hackers Wiki", "https://bash.cyberciti.biz/guide/Exit_codes", "docs"),
      res("set -euo pipefail — dev.to", "https://dev.to/thiht/shell-scripts-matter-5a1g", "article"),
      res("Error handling in Bash — Linux Handbook", "https://linuxhandbook.com/bash-error-handling/", "tutorial"),
    ],
    int: ["What does exit code 127 mean?", "Why is set -e not enough on its own?", "How do you capture a command's exit code?"],
    tips: ["Start every real script with set -euo pipefail"],
    diff: "Intermediate", time: "2 hours",
  },
  "pipes-and-redirection": {
    d: "Pipes (|) connect the stdout of one command to the stdin of the next, letting you chain small tools into powerful pipelines — grep, sort, uniq, awk, xargs and more working as one.",
    why: "Pipelines are the heart of the Unix philosophy: instead of one complex program, compose many simple ones. Most real shell work is building pipelines.",
    obj: ["Chain commands with |", "Filter with grep and select with cut", "Count and deduplicate with wc and uniq", "Transform with sed and awk, pass with xargs"],
    prereq: ["Redirects", "Basic commands"],
    res: [
      res("Pipes — Bash manual", "https://www.gnu.org/software/bash/manual/bash.html#Pipelines", "docs"),
      res("Pipe usage — GeeksforGeeks", "https://www.geeksforgeeks.org/pipes-in-linux/", "tutorial"),
      res("Command-line text processing — Learn X in Y minutes", "https://learnxinyminutes.com/docs/bash/", "reference"),
    ],
    int: ["What's the difference between | and >?", "How do you pipe into a command that ignores stdin?", "When do you need xargs?"],
    tips: ["Build pipelines one stage at a time, checking output as you go"],
    diff: "Intermediate", time: "2 hours",
  },
  "background-and-foreground": {
    d: "Background and foreground process control is how the shell runs jobs: & starts a command in the background, Ctrl+Z suspends it, jobs/fg/bg switch between them, and nohup or disown detach them from the terminal.",
    why: "Long-running commands, dev servers and downloads would otherwise block your terminal; job control is how professionals keep working while things run.",
    obj: ["Start jobs in the background with &", "Suspend and resume with Ctrl+Z, fg, bg", "List jobs with jobs and kill with kill", "Detach with nohup and disown"],
    prereq: ["Basic commands", "Processes"],
    res: [
      res("Job control — Bash manual", "https://www.gnu.org/software/bash/manual/bash.html#Job-Control", "docs"),
      res("Background processes — Linuxize", "https://linuxize.com/post/how-to-run-linux-commands-in-background/", "tutorial"),
      res("nohup explained — Opensource.com", "https://opensource.com/article/20/11/nohup-command", "article"),
    ],
    int: ["What's the difference between & and nohup?", "How do you bring a background job to the foreground?", "What happens to background jobs when the terminal closes?"],
    tips: ["Use nohup ... & with output redirected for anything you want to survive logout"],
    diff: "Intermediate", time: "1–2 hours",
  },
  "core-utilities-grep-awk-sed": {
    d: "The core text utilities — grep for searching, awk for field processing and reporting, sed for stream editing — are the classic Unix text-processing trio that dominates log analysis and data munging.",
    why: "Grep/awk/sed solve most text problems in seconds without writing a program; they are the most reused tools in every sysadmin and DevOps career.",
    obj: ["Search with grep and regex", "Print fields and compute with awk", "Substitute and delete lines with sed", "Combine all three in pipelines"],
    prereq: ["Pipes", "Regex basics"],
    res: [
      res("grep tutorial — DigitalOcean", "https://www.digitalocean.com/community/tutorials/grep-command-in-linux-unix", "tutorial"),
      res("awk tutorial — GeeksforGeeks", "https://www.geeksforgeeks.org/awk-command-unixlinux-examples/", "tutorial"),
      res("sed tutorial — GeeksforGeeks", "https://www.geeksforgeeks.org/sed-command-in-linux-unix-with-examples/", "tutorial"),
    ],
    int: ["How do you grep for a whole word or case-insensitively?", "What's the default field separator in awk?", "How do you replace text in place with sed?"],
    tips: ["Test regex on a sample before running it on the real file"],
    diff: "Intermediate", time: "3–4 hours",
  },
  "scripting-patterns": {
    d: "Scripting patterns are the recurring structures of real shell scripts: argument parsing, loops over files, functions, error handling, and the defensive idioms (set -euo pipefail, temp files, trap cleanup) that make scripts reliable.",
    why: "Knowing the patterns means you write maintainable scripts instead of fragile one-liners that break in production.",
    obj: ["Parse arguments with $@ and shift or getopts", "Loop over files and lists", "Write functions with local variables", "Clean up with trap and temp files"],
    prereq: ["Variables and quoting", "Exit codes", "Functions"],
    res: [
      res("Bash scripting guide — tldp ABS", "https://tldp.org/LDP/abs/html/", "docs"),
      res("Shell scripting best practices — Google style guide", "https://google.github.io/styleguide/shellguide.html", "reference"),
      res("getopts tutorial — ShellHacks", "https://www.shellhacks.com/bash-getopts/", "tutorial"),
    ],
    int: ["How do you loop over files matching a glob?", "What is the trap idiom and what is it for?", "When should a script be split into functions?"],
    tips: ["Keep functions small and name them after what they do"],
    diff: "Intermediate", time: "3 hours",
  },

  // ── systemd ────────────────────────────────────────────────────────────────
  "systemd-units": {
    d: "systemd units are the configuration files that define services, sockets, timers, mounts and targets on modern Linux. Each unit has a [Unit], [Service] or [Install] section describing how and when it runs.",
    why: "systemd is the init system of virtually every modern distro — if you operate Linux servers, you are managing systemd units daily.",
    obj: ["Understand unit types and their sections", "Locate units in /etc/systemd/system", "Enable, disable, start and stop with systemctl", "Write a simple service unit"],
    prereq: ["Basic commands", "File permissions"],
    res: [
      res("Understanding systemd units — DigitalOcean", "https://www.digitalocean.com/community/tutorials/understanding-systemd-units-and-unit-files", "tutorial"),
      res("systemd.unit man page", "https://www.freedesktop.org/software/systemd/man/latest/systemd.unit.html", "docs"),
      res("systemd for admins — Lennart Poettering series", "https://0pointer.net/blog/projects/systemd-for-admins-1.html", "article"),
    ],
    int: ["What's the difference between enable and start?", "How do you make a service restart on failure?", "Where do custom units live?"],
    tips: ["Run systemctl daemon-reload after editing any unit file"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "services-and-timers": {
    d: "Services and timers are the two most-used systemd unit types: services run and supervise a process, timers schedule recurring execution the way cron used to. They are how modern Linux runs everything from web servers to backups.",
    why: "Deploying an app or scheduling a job on Linux means writing a service or timer — this is the operational bread and butter of server work.",
    obj: ["Write a service unit for an app", "Manage services with systemctl", "Schedule with timers and timer units", "Inspect status and logs with journalctl"],
    prereq: ["systemd units", "Writing configuration files"],
    res: [
      res("Creating a systemd service — DigitalOcean", "https://www.digitalocean.com/community/tutorials/how-to-use-systemctl-to-manage-systemd-services-and-units", "tutorial"),
      res("systemd timers — Arch Wiki", "https://wiki.archlinux.org/title/Systemd/Timers", "docs"),
      res("journalctl guide — DigitalOcean", "https://www.digitalocean.com/community/tutorials/how-to-use-journalctl-to-view-and-manipulate-systemd-logs", "tutorial"),
    ],
    int: ["What's the difference between a timer and cron?", "How do you view logs for one service?", "How do you make a service run after network is up?"],
    tips: ["Use timers with OnCalendar= and check with systemctl list-timers"],
    diff: "Intermediate", time: "2–3 hours",
  },

  // ── Networking tools ───────────────────────────────────────────────────────
  "ping-and-traceroute": {
    d: "ping and traceroute are the two classic connectivity diagnostic tools: ping sends ICMP echo requests to test reachability and latency, traceroute maps the path packets take hop by hop.",
    why: "When something 'doesn't connect', these are the first tools you reach for — they isolate whether the problem is local, the network path, or the remote host.",
    obj: ["Use ping to test reachability and measure latency", "Read ping statistics and packet loss", "Use traceroute to map the route", "Interpret which hop is failing"],
    prereq: ["IP addressing", "ICMP basics"],
    res: [
      res("ping command — GeeksforGeeks", "https://www.geeksforgeeks.org/ping-command-in-linux-with-examples/", "tutorial"),
      res("traceroute explained — Cloudflare", "https://www.cloudflare.com/learning/network-layer/what-is-traceroute/", "article"),
      res("Network troubleshooting — Red Hat docs", "https://www.redhat.com/sysadmin/network-troubleshooting-basics", "article"),
    ],
    int: ["What does 100% packet loss tell you?", "What's the difference between ping and traceroute?", "Why might ping fail while a website loads?"],
    tips: ["Many hosts block ICMP — a failed ping isn't always a dead host"],
    diff: "Beginner", time: "1–2 hours",
  },
  "netstat-and-ss": {
    d: "netstat and ss are the tools that show sockets, open ports and active connections on a Linux host. ss is the modern replacement; netstat is the classic still found everywhere.",
    why: "Every 'which process owns this port?' and 'what is this server talking to?' question is answered with these tools — essential for troubleshooting and security.",
    obj: ["List listening ports with ss -tlnp", "Show established connections", "Find the process behind a port", "Use netstat's classic flags"],
    prereq: ["TCP/UDP basics", "Ports and protocols"],
    res: [
      res("ss command explained — Red Hat", "https://www.redhat.com/sysadmin/ss-command", "article"),
      res("netstat vs ss — Opensource.com", "https://opensource.com/article/20/3/netstat-ss", "article"),
      res("ss man page — man7", "https://man7.org/linux/man-pages/man8/ss.8.html", "docs"),
    ],
    int: ["How do you find which process is using port 8080?", "What's the difference between LISTEN and ESTABLISHED?", "Why is ss preferred over netstat?"],
    tips: ["ss -tulpn shows listening TCP/UDP ports with process names"],
    diff: "Intermediate", time: "1–2 hours",
  },
  "ip-and-ifconfig": {
    d: "ip and ifconfig are the tools that inspect and configure network interfaces — addresses, routes, link state. ip is the modern command family; ifconfig is its legacy predecessor.",
    why: "Checking a server's IP, bringing an interface up, or understanding routing all start here — the ip command is the first thing to learn about network config.",
    obj: ["Show addresses with ip addr", "Show routes with ip route", "Bring interfaces up and down", "Translate ifconfig habits to ip"],
    prereq: ["IP addressing", "Interfaces"],
    res: [
      res("ip command cheat sheet — Red Hat", "https://www.redhat.com/sysadmin/ip-command-cheat-sheet", "reference"),
      res("ip vs ifconfig — Opensource.com", "https://opensource.com/article/20/3/ip-command", "article"),
      res("IP command tutorial — GeeksforGeeks", "https://www.geeksforgeeks.org/ip-command-in-linux-with-examples/", "tutorial"),
    ],
    int: ["How do you list all IP addresses of a machine?", "What's the difference between ip addr and ip link?", "Why is ifconfig deprecated?"],
    tips: ["ip addr, ip route and ip link cover 90% of daily use"],
    diff: "Beginner", time: "1–2 hours",
  },
  "curl": {
    d: "curl is the command-line tool for transferring data with URLs — HTTP requests, downloads, API calls and more. It is the universal way to talk to web services from a terminal.",
    why: "Testing an API, debugging a webhook, downloading a file or checking a server's response — curl is the first tool in every developer and DevOps kit.",
    obj: ["Make GET and POST requests", "Send headers and JSON bodies", "Follow redirects with -L", "Inspect response headers and status codes"],
    prereq: ["HTTP basics", "Command line"],
    res: [
      res("curl tutorial — GeeksforGeeks", "https://www.geeksforgeeks.org/curl-command-in-linux-with-examples/", "tutorial"),
      res("curl documentation — curl.se", "https://curl.se/docs/manpage.html", "docs"),
      res("REST APIs with curl — DigitalOcean", "https://www.digitalocean.com/community/tutorials/how-to-interact-with-web-services-using-curl", "tutorial"),
    ],
    int: ["How do you send a JSON POST with curl?", "What does -L do and when do you need it?", "How do you see only the response headers?"],
    tips: ["Use curl -i to see status and headers alongside the body"],
    diff: "Beginner", time: "2 hours",
  },
  "packet-analysis-tcpdump": {
    d: "Packet analysis with tcpdump is capturing and inspecting raw network traffic — the actual packets crossing an interface — to diagnose protocol issues, performance problems and security anomalies.",
    why: "When higher-level tools stop making sense, packets are the ground truth; tcpdump is the standard way to see them.",
    obj: ["Capture traffic on an interface with tcpdump", "Filter by host, port and protocol", "Save captures with -w and read with -r", "Read basic packet output"],
    prereq: ["TCP/IP", "Ports and protocols"],
    res: [
      res("tcpdump tutorial — Daniel Miessler", "https://danielmiessler.com/p/tcpdump/", "tutorial"),
      res("tcpdump man page — tcpdump.org", "https://www.tcpdump.org/manpages/tcpdump.1.html", "docs"),
      res("Analyzing network traffic — DigitalOcean", "https://www.digitalocean.com/community/tutorials/how-to-use-tcpdump-to-analyze-network-traffic", "tutorial"),
    ],
    int: ["How do you capture only HTTP traffic on port 80?", "What does a TCP handshake look like in a capture?", "Why save captures with -w before analyzing?"],
    tips: ["Always narrow with -i and filters — full captures are huge"],
    diff: "Advanced", time: "3 hours",
  },

  // ── Git / open source ──────────────────────────────────────────────────────
  "versioning": {
    d: "Versioning is the practice of naming and tracking software releases — semantic versioning (MAJOR.MINOR.PATCH), release tags and changelogs that tell users and tools what changed and how breaking it is.",
    why: "Version numbers are a contract: they tell every dependent project whether an upgrade is safe, and they make releases reproducible and debuggable.",
    obj: ["Understand semver MAJOR.MINOR.PATCH", "Choose when to bump each part", "Tag releases with git tag", "Write release notes and changelogs"],
    prereq: ["Git basics", "Releases"],
    res: [
      res("Semantic Versioning — semver.org", "https://semver.org/", "docs"),
      res("Versioning guide — GitLab docs", "https://docs.gitlab.com/development/versioning/", "docs"),
      res("Release management — GitHub docs", "https://docs.github.com/en/repositories/releasing-projects-on-github/managing-releases-in-a-repository", "docs"),
    ],
    int: ["When do you bump MAJOR versus MINOR?", "What is 0.x.x supposed to signal?", "How do breaking changes affect versioning?"],
    tips: ["Tag every release — tags are the roadmap of your versions"],
    diff: "Beginner", time: "1–2 hours",
  },
  "good-first-issues": {
    d: "Good first issues are beginner-friendly tasks maintainers label in open-source repositories — small, well-scoped, documented bugs or features designed for a first-time contributor to tackle.",
    why: "They are the standard on-ramp to open source: a low-risk way to learn a project's workflow, get a merged PR and build contribution history.",
    obj: ["Find good-first-issue labels on GitHub", "Read the project's contributing guide", "Pick an issue you can actually complete", "Claim it and ask clarifying questions"],
    prereq: ["Git basics", "Pull requests"],
    res: [
      res("How to contribute to open source — GitHub", "https://opensource.guide/how-to-contribute/", "article"),
      res("Finding good first issues — GitHub docs", "https://docs.github.com/en/get-started/exploring-projects-on-github/finding-ways-to-contribute-to-open-source-on-github", "docs"),
      res("First-timers-only — issue tracker", "https://www.firsttimersonly.com/", "practice"),
    ],
    int: ["How do you pick a good first issue?", "What should you do before claiming an issue?", "How do you get help without being a nuisance?"],
    tips: ["Comment on the issue to signal you're working on it before you start"],
    diff: "Beginner", time: "2–4 hours",
  },
  "submitting-prs": {
    d: "Submitting pull requests is the full contribution workflow: forking, branching, committing, pushing, opening the PR, responding to review and iterating until merge. It is how changes enter shared codebases.",
    why: "The PR is the unit of collaboration in modern software — reviews, CI and deployment all hang off it, and submitting clean PRs is a core professional skill.",
    obj: ["Fork and clone a repository", "Create a focused branch", "Write a clear PR description", "Respond to review comments and rebase"],
    prereq: ["Git branching", "Version control"],
    res: [
      res("About pull requests — GitHub docs", "https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/about-pull-requests", "docs"),
      res("Creating a PR — GitHub docs", "https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/creating-a-pull-request", "docs"),
      res("The open-source guide — first contributions", "https://opensource.guide/", "article"),
    ],
    int: ["What belongs in a good PR description?", "How do you handle a requested change on your PR?", "What's the difference between rebase and merge?"],
    tips: ["Keep each PR small and focused on one change"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "actions-basics": {
    d: "GitHub Actions basics are the foundation of CI/CD on GitHub: workflows defined in YAML with triggers, jobs, steps and actions that run on events like push and pull request.",
    why: "Actions is how most open-source and many commercial projects automate tests, builds and deploys — reading and writing workflows is a standard developer skill.",
    obj: ["Write a workflow YAML file", "Trigger on push and pull_request", "Define jobs, steps and run commands", "Use marketplace actions and secrets"],
    prereq: ["Git basics", "YAML"],
    res: [
      res("Understanding GitHub Actions — GitHub docs", "https://docs.github.com/en/actions/about-github-actions/understanding-github-actions", "docs"),
      res("Workflow syntax — GitHub docs", "https://docs.github.com/en/actions/reference/workflow-syntax-for-github-actions", "docs"),
      res("Learn GitHub Actions — freeCodeCamp", "https://www.freecodecamp.org/news/github-actions-for-beginners/", "tutorial"),
    ],
    int: ["What's the difference between a job and a step?", "How do you pass a secret to a step?", "When does a workflow run?"],
    tips: ["Start with a minimal workflow that just echoes, then grow it"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "breaking-changes": {
    d: "Breaking changes are modifications that stop existing consumers from working — renamed functions, removed endpoints, changed schemas. Managing them is a discipline: deprecate, announce, then remove on a major version.",
    why: "Breaking changes silently is how libraries and APIs destroy trust; handled well, they are a normal, scheduled part of a healthy project's evolution.",
    obj: ["Identify what counts as breaking", "Deprecate before removing", "Announce in changelogs and migration guides", "Ship breaking changes in major versions"],
    prereq: ["Versioning", "Semver"],
    res: [
      res("Semver and breaking changes — semver.org", "https://semver.org/#what-does-0-2-3-mean", "docs"),
      res("Writing migration guides — Stripe blog", "https://stripe.com/blog/api-versioning", "article"),
      res("API deprecation policy — Google docs", "https://cloud.google.com/apis/docs/deprecation", "docs"),
    ],
    int: ["What counts as a breaking change?", "How long should a deprecation window be?", "How do you communicate a breaking change?"],
    tips: ["Deprecate loudly and document the migration path before removal"],
    diff: "Intermediate", time: "1–2 hours",
  },
  "scopes": {
    d: "Scopes in a commit message are the part of the Conventional Commits prefix that names the area touched — feat(api), fix(parser), docs(readme) — giving each change a searchable subject area.",
    why: "Well-scoped commits make history readable, enable automated changelogs and help reviewers understand a change's impact at a glance.",
    obj: ["Write conventional commit prefixes", "Pick meaningful scope names", "Keep the subject line short", "Use scopes for automated changelogs"],
    prereq: ["Git commits", "Commit hygiene"],
    res: [
      res("Conventional Commits spec", "https://www.conventionalcommits.org/en/v1.0.0/", "docs"),
      res("Writing good commits — freeCodeCamp", "https://www.freecodecamp.org/news/writing-good-commit-messages/", "article"),
      res("Git commit messages — cbeams style guide", "https://cbea.ms/git-commit/", "article"),
    ],
    int: ["What's the difference between feat and fix?", "What makes a good scope?", "Why keep the subject under 50 characters?"],
    tips: ["Pick scopes from the parts of your codebase that change most"],
    diff: "Beginner", time: "1 hour",
  },
  "changelogs": {
    d: "A changelog is a curated, human-readable list of changes per release — usually following Keep a Changelog: Added, Changed, Deprecated, Removed, Fixed, Security. It is the first thing users check before upgrading.",
    why: "Changelogs turn a tarball of commits into a story users can trust, and they are the standard place to announce breaking changes and security fixes.",
    obj: ["Structure entries with Keep a Changelog", "Link entries to issues and PRs", "Update the changelog with every release", "Announce breaking changes prominently"],
    prereq: ["Versioning", "Release notes"],
    res: [
      res("Keep a Changelog — keepachangelog.com", "https://keepachangelog.com/en/1.1.0/", "reference"),
      res("Changelog automation with standard-version", "https://github.com/conventional-changelog/standard-version", "docs"),
      res("Semantic-release docs", "https://semantic-release.gitbook.io/semantic-release/", "docs"),
    ],
    int: ["What belongs in a changelog versus release notes?", "Who is a changelog for?", "How do you automate changelog generation?"],
    tips: ["Write changelog entries as you merge PRs, not at release time"],
    diff: "Beginner", time: "1 hour",
  },

  // ── Deployment / CI ────────────────────────────────────────────────────────
  "environment-promotion": {
    d: "Environment promotion is moving a build through stages — dev, staging, production — with the same artifact, varying only configuration. It is what makes releases predictable and rollbacks safe.",
    why: "Promoting the identical build you tested is the core guarantee that what's in production is what you validated — config-only differences eliminate 'works on staging' surprises.",
    obj: ["Model dev, staging and production", "Promote the same artifact between them", "Keep configuration external and env-specific", "Gate promotion on passing checks"],
    prereq: ["CI/CD basics", "Deployment"],
    res: [
      res("Environments — GitHub docs", "https://docs.github.com/en/actions/deployment/using-environments-for-deployment", "docs"),
      res("12-factor — config in the environment", "https://12factor.net/config", "docs"),
      res("Deployment strategies — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/patterns/category/deployment-stability", "docs"),
    ],
    int: ["Why promote the same artifact instead of rebuilding?", "What should differ between environments?", "How do you gate a promotion?"],
    tips: ["Build once, promote many — never rebuild for production"],
    diff: "Intermediate", time: "2 hours",
  },
  "stages-and-gates": {
    d: "Stages and gates structure a CI/CD pipeline: stages (build, test, deploy) run in order, and gates (test results, approvals, time windows) decide whether the pipeline proceeds to the next stage.",
    why: "Gates are what stop broken code from reaching users — they turn a pipeline from a conveyor belt into a controlled release process.",
    obj: ["Design pipeline stages", "Fail fast in early stages", "Add approval gates for production", "Understand when gates are manual vs automatic"],
    prereq: ["CI/CD basics", "Pipelines"],
    res: [
      res("Pipelines and stages — GitLab docs", "https://docs.gitlab.com/ci/pipelines/", "docs"),
      res("Deployment environments and gates — Azure DevOps", "https://learn.microsoft.com/en-us/azure/devops/pipelines/process/approvals?view=azure-devops", "docs"),
      res("CI/CD pipeline stages — Red Hat", "https://www.redhat.com/en/topics/devops/what-cicd-pipeline", "article"),
    ],
    int: ["What's the difference between a stage and a step?", "When should an approval gate be manual?", "How do you fail a pipeline fast?"],
    tips: ["Run the cheapest checks first so failures surface early"],
    diff: "Intermediate", time: "2 hours",
  },
  "rollback-strategies": {
    d: "Rollback strategies are the predefined ways to undo a bad deployment: redeploy the previous version, or use release patterns that make reversal instant and safe.",
    why: "Every deployment can fail — the difference between a blip and an outage is having a rehearsed, fast way to get back to the last known-good state.",
    obj: ["Keep the previous version deployable", "Roll back a container image or artifact", "Use feature flags to disable without redeploying", "Rehearse rollback on staging"],
    prereq: ["Deployment", "Release management"],
    res: [
      res("Deployment strategies — Kubernetes docs", "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#revision-history-limit", "docs"),
      res("Rollback patterns — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/patterns/", "docs"),
      res("How to roll back a deployment — GitLab docs", "https://docs.gitlab.com/ee/ci/environments/", "docs"),
    ],
    int: ["What's the fastest rollback you can have?", "When is redeploying the old version not enough?", "How do feature flags help rollback?"],
    tips: ["Practice rollback on staging before you ever need it in production"],
    diff: "Advanced", time: "2 hours",
  },
  "rolling-and-canary": {
    d: "Rolling and canary are progressive deployment patterns: rolling updates replace instances gradually (e.g. 25% at a time), canaries route a small slice of real traffic to the new version before full rollout.",
    why: "They shrink the blast radius of a bad release — instead of all users hitting the bug, only a fraction do, and you can halt the rollout.",
    obj: ["Deploy rolling with maxSurge/maxUnavailable", "Shift a percentage of traffic to a canary", "Watch metrics before promoting", "Automate or abort the rollout"],
    prereq: ["Deployment", "Observability"],
    res: [
      res("Rolling update — Kubernetes docs", "https://kubernetes.io/docs/tutorials/kubernetes-basics/update/update-intro/", "tutorial"),
      res("Canary deployments — Google Cloud docs", "https://cloud.google.com/architecture/deploying-to-production-using-canary-deployments", "docs"),
      res("Deployment strategies — Octopus blog", "https://octopus.com/docs/deployments/patterns/canary-deployments", "article"),
    ],
    int: ["What's the difference between rolling and canary?", "How do you decide canary traffic percentage?", "What metrics tell you to abort a canary?"],
    tips: ["Combine canary with automated metric checks to self-abort"],
    diff: "Advanced", time: "2–3 hours",
  },
  "rollback-procedures": {
    d: "Rollback procedures are the documented, step-by-step runbooks for reverting a deployment — the exact commands, in the exact order, with success criteria, so any on-call engineer can execute them under pressure.",
    why: "In an incident you don't think, you execute — a pre-written rollback runbook is the difference between a 5-minute recovery and a 5-hour scramble.",
    obj: ["Write a rollback runbook", "Document the exact commands and order", "Define success criteria", "Test the runbook regularly"],
    prereq: ["Deployment", "Incident response"],
    res: [
      res("Incident response runbooks — Atlassian", "https://www.atlassian.com/incident-management/runbooks", "article"),
      res("Deployment runbook template — PagerDuty", "https://www.pagerduty.com/resources/guides/runbook/", "article"),
      res("Rollback guide — Google SRE workbook", "https://sre.google/workbook/", "docs"),
    ],
    int: ["What must a rollback runbook contain?", "How often should runbooks be tested?", "Who should be able to execute a rollback?"],
    tips: ["Store runbooks with the deployment code so they stay in sync"],
    diff: "Advanced", time: "1–2 hours",
  },
  "canary-releases": {
    d: "Canary releases ship a new version to a small subset of users or traffic first, compare behavior against the old version, and only then roll out broadly — named after the canary in the coal mine.",
    why: "Canaries catch problems with real traffic before the majority of users see them, making them the strongest safety net for high-stakes releases.",
    obj: ["Route a small traffic slice to the canary", "Compare key metrics between versions", "Promote or roll back based on signals", "Automate the promotion decision"],
    prereq: ["Deployment", "Metrics"],
    res: [
      res("Canary deployment — Martin Fowler", "https://martinfowler.com/bliki/CanaryRelease.html", "article"),
      res("Canary analysis — Argo Rollouts docs", "https://argoproj.github.io/argo-rollouts/features/canary/", "docs"),
      res("Canary deployments — AWS docs", "https://docs.aws.amazon.com/whitepapers/latest/practicing-continuous-integration-continuous-delivery/canary-deployments.html", "docs"),
    ],
    int: ["How small should the canary slice start?", "What metrics decide promote vs abort?", "How do you avoid canary skew (different user mix)?"],
    tips: ["Watch error rate and latency, not just HTTP status"],
    diff: "Advanced", time: "2 hours",
  },
  "traffic-shifting": {
    d: "Traffic shifting is controlling what fraction of requests reach which version of a service — via load balancers, service meshes or feature flags — to enable canaries, blue-green swaps and gradual cutovers.",
    why: "Shifting traffic is the mechanism behind every progressive deployment; mastering it means you can move users to new software at any speed you choose.",
    obj: ["Shift traffic by percentage at the LB", "Use weighted routing in a service mesh", "Shift with feature flags in the app", "Observe impact per slice"],
    prereq: ["Load balancing", "Deployment"],
    res: [
      res("Traffic management — Istio docs", "https://istio.io/latest/docs/concepts/traffic-management/", "docs"),
      res("Weighted routing — AWS ALB docs", "https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-target-groups.html", "docs"),
      res("Feature flags and traffic — LaunchDarkly docs", "https://docs.launchdarkly.com/home/getting-started/understanding-flags", "docs"),
    ],
    int: ["What's the difference between traffic shifting and blue-green?", "How do you shift traffic without dropping sessions?", "When is a feature flag better than LB routing?"],
    tips: ["Shift in small steps and watch error rates at each step"],
    diff: "Advanced", time: "2 hours",
  },
  "runners": {
    d: "Runners are the machines that execute CI/CD jobs — GitHub-hosted or self-hosted agents that pull a job, run the steps and report results. Runner pools, labels and concurrency shape how fast your pipeline runs.",
    why: "Pipeline speed, cost and security all hinge on runners: where they live, what they can access and how they scale.",
    obj: ["Understand hosted vs self-hosted runners", "Use runner labels to select environments", "Secure self-hosted runners", "Scale runners for build queues"],
    prereq: ["CI/CD basics", "GitHub Actions or GitLab CI"],
    res: [
      res("About self-hosted runners — GitHub docs", "https://docs.github.com/en/actions/hosting-your-own-runners/about-self-hosted-runners", "docs"),
      res("Runners — GitLab docs", "https://docs.gitlab.com/runner/", "docs"),
      res("Runner groups — Azure DevOps docs", "https://learn.microsoft.com/en-us/azure/devops/pipelines/agents/agent-groups?view=azure-devops", "docs"),
    ],
    int: ["What's the difference between hosted and self-hosted runners?", "How do labels route jobs to the right runner?", "What are the security risks of self-hosted runners?"],
    tips: ["Use hosted runners by default; self-host only when you need special hardware"],
    diff: "Intermediate", time: "2 hours",
  },
  "caching-and-artifacts": {
    d: "Caching and artifacts are the two ways CI stores things between runs: caches speed up dependency installs (node_modules, pip), artifacts carry build outputs between jobs and stages (binaries, reports, images).",
    why: "Good caching cuts pipeline time dramatically, and artifacts are how a build's output reaches later stages and deployments — both are core CI performance skills.",
    obj: ["Cache package dependencies in CI", "Upload and download artifacts between jobs", "Key caches on the right inputs", "Avoid caching secrets and stale data"],
    prereq: ["CI/CD basics", "Build processes"],
    res: [
      res("Caching dependencies — GitHub docs", "https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows", "docs"),
      res("Artifacts — GitLab docs", "https://docs.gitlab.com/ci/pipelines/job_artifacts/", "docs"),
      res("Cache and artifacts — Azure DevOps", "https://learn.microsoft.com/en-us/azure/devops/pipelines/release/caching?view=azure-devops", "docs"),
    ],
    int: ["What's the difference between a cache and an artifact?", "Why do caches go stale?", "How do you avoid caching secrets?"],
    tips: ["Only cache what's expensive to rebuild and changes rarely"],
    diff: "Intermediate", time: "2 hours",
  },
  "registry-and-ci": {
    d: "Registry and CI is the workflow of building a container image, pushing it to a container registry (Docker Hub, GHCR, ECR), and pulling it in CI/CD for deployment — the artifact pipeline of modern apps.",
    why: "Images are the deployment unit of modern infrastructure; a clean registry/CI flow means reproducible builds and fast, safe deploys.",
    obj: ["Build images with Docker or BuildKit", "Push to a container registry", "Tag with commit SHA and version", "Pull and deploy from the registry in CI"],
    prereq: ["Docker basics", "CI/CD"],
    res: [
      res("Publishing images — GitHub docs", "https://docs.github.com/en/packages/working-with-a-github-packages-registry/working-with-the-container-registry", "docs"),
      res("Best practices for building images — Docker", "https://docs.docker.com/develop/develop-images/dockerfile_best-practices/", "docs"),
      res("Build, push, deploy — GitLab docs", "https://docs.gitlab.com/ci/docker/using_docker_build/", "docs"),
    ],
    int: ["Why tag images with the commit SHA?", "What's the difference between latest and a SHA tag?", "How do you secure a registry?"],
    tips: ["Tag every image with its commit SHA, not just latest"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "compose-files-and-services": {
    d: "Compose files describe multi-container applications in YAML: the services, networks, volumes, environment and ports — so `docker compose up` starts the whole stack consistently.",
    why: "Compose is how local dev, tests and small deployments run entire applications — databases, caches and apps together — from one file anyone can read.",
    obj: ["Define services, networks and volumes in YAML", "Wire environment and ports", "Use depends_on and healthchecks", "Run and manage with docker compose commands"],
    prereq: ["Docker basics", "YAML"],
    res: [
      res("Compose file reference — Docker docs", "https://docs.docker.com/compose/compose-file/", "docs"),
      res("Getting started with Compose — Docker docs", "https://docs.docker.com/compose/gettingstarted/", "tutorial"),
      res("Compose in production — Docker docs", "https://docs.docker.com/compose/production/", "docs"),
    ],
    int: ["What's the difference between a service and a container?", "When should a database be a compose service?", "How do you persist data across restarts?"],
    tips: ["Pin image versions and use named volumes for data"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "environment-configuration": {
    d: "Environment configuration is managing the settings that differ between environments — URLs, credentials, feature flags — without rebuilding the app, usually via environment variables and config files.",
    why: "Config in the environment (12-factor) is what lets the same artifact run in dev, staging and production — the foundation of safe deployments.",
    obj: ["Keep config out of code", "Use environment variables and .env files", "Manage secrets separately from config", "Validate configuration at startup"],
    prereq: ["12-factor principles", "Deployment"],
    res: [
      res("Config in the environment — 12factor.net", "https://12factor.net/config", "docs"),
      res("Environment variables — Node.js docs", "https://nodejs.org/en/learn/command-line/how-to-read-environment-variables-from-nodejs", "docs"),
      res("Secrets management — Vault docs", "https://developer.hashicorp.com/vault/docs/concepts/secrets-engines", "docs"),
    ],
    int: ["Why should config live in the environment, not the code?", "What's the difference between config and secrets?", "How do you validate config fails loudly?"],
    tips: ["Fail fast at startup if required configuration is missing"],
    diff: "Intermediate", time: "1–2 hours",
  },
  "scaling-and-health-checks": {
    d: "Scaling and health checks are how running services grow and stay healthy: horizontal scaling adds replicas, health/readiness probes tell the platform when a container is ready or broken, and autoscaling reacts to load.",
    why: "Health checks are what make self-healing possible, and scaling is how you meet demand — both are the daily mechanics of running production workloads.",
    obj: ["Add replicas and understand horizontal scaling", "Write liveness and readiness probes", "Configure autoscaling on CPU or custom metrics", "Set resource requests and limits"],
    prereq: ["Containers", "Kubernetes or Compose basics"],
    res: [
      res("Configure probes — Kubernetes docs", "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/", "docs"),
      res("Horizontal Pod Autoscaler — Kubernetes docs", "https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/", "docs"),
      res("Health checks — Docker docs", "https://docs.docker.com/engine/reference/builder/#healthcheck", "docs"),
    ],
    int: ["What's the difference between liveness and readiness?", "Why set resource requests and limits?", "What happens when a health check fails?"],
    tips: ["Make readiness depend on the app's real dependencies, not just the process"],
    diff: "Advanced", time: "2–3 hours",
  },
  "usecases-and-benefits": {
    d: "Use cases and benefits is the section that maps a technology to the real problems it solves and the advantages it brings — so you can say precisely when to reach for it and what it buys you.",
    why: "Interviewers and stakeholders ask 'why this tool?' constantly; being able to articulate use cases and trade-offs is what separates tool users from engineers.",
    obj: ["Describe the technology's primary use cases", "Articulate its benefits over alternatives", "Name its limits and when not to use it", "Give a concrete example of each use case"],
    prereq: ["The basics of the technology"],
    res: [
      res("Writing about technology trade-offs — Martin Fowler", "https://martinfowler.com/bliki/TradeoffAnalysis.html", "article"),
      res("Choosing technology — thoughtworks radar", "https://www.thoughtworks.com/radar", "article"),
      res("Explain use cases — Wikipedia style guide", "https://en.wikipedia.org/wiki/Use_case", "docs"),
    ],
    int: ["What problem does this technology solve best?", "What are its main drawbacks?", "When would you choose it over a competitor?"],
    tips: ["Learn the one killer use case first, then the trade-offs"],
    diff: "Beginner", time: "1 hour",
  },
  "cac-vs-iac": {
    d: "CaC vs IaC is the comparison between configuration as code (declaring the desired state of systems — Ansible, Puppet, Chef) and infrastructure as code (declaring infrastructure itself — Terraform, CloudFormation).",
    why: "The two solve different problems — what runs on the machine vs what machines exist — and knowing which to reach for is a core DevOps design decision.",
    obj: ["Define IaC and CaC precisely", "Compare declarative vs imperative tools", "Understand when each applies", "Combine them in a real pipeline"],
    prereq: ["DevOps fundamentals", "Cloud basics"],
    res: [
      res("What is infrastructure as code? — Red Hat", "https://www.redhat.com/en/topics/automation/what-is-infrastructure-as-code-iac", "article"),
      res("Terraform vs Ansible — Spacelift", "https://spacelift.io/blog/terraform-vs-ansible", "article"),
      res("IaC best practices — HashiCorp", "https://developer.hashicorp.com/terraform/tutorials/configuration-language", "docs"),
    ],
    int: ["What's the difference between IaC and CaC?", "Is Ansible IaC or CaC — and why does it matter?", "How do Terraform and Ansible complement each other?"],
    tips: ["Use Terraform for infrastructure, Ansible for what runs on it"],
    diff: "Intermediate", time: "1–2 hours",
  },
};
