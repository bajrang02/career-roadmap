// ─────────────────────────────────────────────────────────────────────────────
// Curated knowledge for the most-recurring security / observability / cloud /
// DevOps / CI-CD concept labels. One entry per label applies in every roadmap
// where that label appears. Merged into ALL_KNOWLEDGE in generate.mjs
// (KNOWLEDGE wins, so nothing hand-authored elsewhere is overridden).
// Entries stay atomic: META_SLUGS keeps their objectives from spawning
// thin concept children.
// ─────────────────────────────────────────────────────────────────────────────

const res = (t, u, k) => ({ t, u, k });

export const META_RECURRING_B = {
  // ── Security ───────────────────────────────────────────────────────────────
  "broken-access-control": {
    d: "Broken access control is the OWASP #1 web risk: when a user can access functions or data they were not authorised for — missing checks on object IDs (IDOR), missing function-level checks, or forced browsing of protected URLs.",
    why: "It is the most common serious web vulnerability because it's easy to forget an authorization check and hard to notice it's missing — and it leads straight to data breaches.",
    obj: ["Understand IDOR and object-level authorization", "Enforce function-level access checks server-side", "Test every endpoint for authorization, not just authentication", "Apply deny-by-default and least privilege"],
    prereq: ["Authentication vs authorization", "HTTP and web basics"],
    res: [
      res("Access control — OWASP", "https://owasp.org/www-community/Access_Control", "docs"),
      res("Broken access control — OWASP Top 10", "https://owasp.org/Top10/A01_2021-Broken_Access_Control/", "docs"),
      res("IDOR — PortSwigger", "https://portswigger.net/web-security/access-control/idor", "tutorial"),
    ],
    int: ["What is IDOR and how do you find it?", "Why is access control a design problem, not a code problem?", "How do you test authorization at every layer?"],
    tips: ["Test authorization with two accounts of different roles on every endpoint"],
    diff: "Advanced", time: "4–5 hours",
  },
  "cryptographic-failures": {
    d: "Cryptographic failures are the OWASP #2 risk: using weak or outdated cryptography, hard-coding secrets, or transporting sensitive data without encryption — the places where crypto is absent, misused or undermined.",
    why: "Modern crypto done right is effectively unbreakable; virtually every real crypto failure is a deployment or design mistake, not a broken algorithm.",
    obj: ["Classify data and protect it in transit and at rest", "Use current, vetted algorithms and libraries", "Manage secrets with dedicated tools, never code", "Avoid rolling your own crypto"],
    prereq: ["Symmetric vs asymmetric encryption", "TLS basics"],
    res: [
      res("Cryptographic failures — OWASP Top 10", "https://owasp.org/Top10/A02_2021-Cryptographic_Failures/", "docs"),
      res("Cryptography — OWASP cheat sheet", "https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html", "reference"),
      res("Secret management — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html", "reference"),
    ],
    int: ["Why is HTTPS everywhere no longer optional?", "What is the difference between encryption at rest and in transit?", "How should secrets be stored and rotated?"],
    tips: ["Use TLS 1.2+, AEAD ciphers, and a secrets manager — never invent crypto"],
    diff: "Advanced", time: "4–5 hours",
  },
  "injection": {
    d: "Injection is the OWASP #3 risk: attacker-supplied data executed as code — SQL injection in queries, command injection in shells, LDAP, XPath and template injection. It happens whenever untrusted input is concatenated into an interpreted context.",
    why: "Injection is the classic 'how hackers take over databases' vulnerability, and it still tops breach reports decades after being documented.",
    obj: ["Recognize SQL, command and template injection", "Use parameterized queries and prepared statements", "Validate and allowlist untrusted input", "Test with safe payloads and scanners"],
    prereq: ["SQL basics", "HTTP request handling"],
    res: [
      res("SQL injection — OWASP", "https://owasp.org/www-community/attacks/SQL_Injection", "docs"),
      res("SQL injection — PortSwigger", "https://portswigger.net/web-security/sql-injection", "tutorial"),
      res("Injection — OWASP Top 10", "https://owasp.org/Top10/A03_2021-Injection/", "docs"),
    ],
    int: ["How does a parameterized query stop SQL injection?", "What is the difference between validation and sanitization?", "Where does command injection happen?"],
    tips: ["Never build queries by string concatenation — parameterize everything"],
    diff: "Advanced", time: "4–5 hours",
  },
  "insecure-design": {
    d: "Insecure design is the OWASP #4 risk: vulnerabilities that exist because the system's design — threat model, trust boundaries, business logic — is flawed before a line of code is written, rather than because of a coding mistake.",
    why: "Design-level flaws are the hardest to fix because they're baked into the architecture; threat modelling and secure defaults prevent them at the cheapest possible point.",
    obj: ["Build a threat model before coding", "Apply secure defaults and fail-closed design", "Review business logic for abuse cases", "Use security requirements in the design phase"],
    prereq: ["Basic security principles", "System design basics"],
    res: [
      res("Insecure design — OWASP Top 10", "https://owasp.org/Top10/A04_2021-Insecure_Design/", "docs"),
      res("Threat modeling — OWASP", "https://owasp.org/www-community/Threat_Modeling", "docs"),
      res("Threat modeling — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/security/develop/threat-modeling-tool", "docs"),
    ],
    int: ["What is the difference between insecure design and insecure implementation?", "How does threat modeling find flaws early?", "What are secure defaults?"],
    tips: ["Threat-model the user flows, not just the code paths"],
    diff: "Advanced", time: "4–5 hours",
  },
  "security-misconfiguration": {
    d: "Security misconfiguration is the OWASP #5 risk: systems left insecure through default settings — unpatched software, verbose error messages, open storage, enabled debug modes, default credentials and missing security headers.",
    why: "It's the most common vulnerability in practice because it's boring: a default admin password or an open S3 bucket doesn't need a clever exploit.",
    obj: ["Harden defaults and remove debug features", "Apply security headers and correct error handling", "Automate patching and configuration scanning", "Lock down cloud storage and ports"],
    prereq: ["Basic security awareness", "Deployment basics"],
    res: [
      res("Security misconfiguration — OWASP Top 10", "https://owasp.org/Top10/A05_2021-Security_Misconfiguration/", "docs"),
      res("Configuration guides — CIS", "https://www.cisecurity.org/cis-benchmarks", "docs"),
      res("Security headers — MDN Web Docs", "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers", "reference"),
    ],
    int: ["What are the most common misconfigurations?", "How do security headers help?", "Why should error messages be generic?"],
    tips: ["Baseline every server against CIS benchmarks and scan regularly"],
    diff: "Intermediate", time: "3–4 hours",
  },
  "how-to-mitigate-each": {
    d: "Mitigating each vulnerability in the OWASP Top 10 is the practical defence playbook: for every risk category, the concrete controls that prevent it — parameterization for injection, access checks for broken access control, headers and CSP for XSS, patching for misconfiguration.",
    why: "Knowing the risk is awareness; knowing the mitigation is skill — and interviewers ask 'how would you fix this?' precisely to test the second half.",
    obj: ["Pair each Top 10 risk with its primary defence", "Apply layered controls (prevent, detect, respond)", "Fix vulnerabilities in order of risk", "Verify mitigations with retests"],
    prereq: ["The OWASP Top 10 risk categories"],
    res: [
      res("OWASP Top 10 — full list", "https://owasp.org/Top10/", "docs"),
      res("OWASP cheat sheets", "https://cheatsheetseries.owasp.org/", "reference"),
      res("Secure coding practices — OWASP", "https://owasp.org/www-project-secure-coding-practices-quick-reference-guide/", "reference"),
    ],
    int: ["Pick a Top 10 risk and walk me through its mitigation.", "What is defence in depth?", "How do you prioritize which vulnerabilities to fix first?"],
    tips: ["Learn one mitigation per risk category cold — that covers 90% of interview answers"],
    diff: "Advanced", time: "4–6 hours",
  },
  "sessions-cookies": {
    d: "Sessions and cookies are how web apps remember who you are between requests: the server stores session state, the browser carries a session cookie with a random ID, and both sides validate it on every request.",
    why: "Almost every web application is built on session management, and its flaws — predictable IDs, stolen cookies, fixation — are a classic security topic.",
    obj: ["Explain the session cookie flow", "Generate unpredictable session IDs", "Protect cookies with Secure, HttpOnly, SameSite", "Invalidate sessions on logout"],
    prereq: ["HTTP basics", "Authentication"],
    res: [
      res("Session management — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html", "reference"),
      res("HTTP cookies — MDN Web Docs", "https://developer.mozilla.org/en-US/docs/Web/HTTP/Cookies", "docs"),
      res("Sessions explained — Auth0", "https://auth0.com/blog/cookies-vs-tokens-definitive-guide/", "article"),
    ],
    int: ["What is the difference between sessions and tokens?", "Why set HttpOnly on cookies?", "What is session fixation?"],
    tips: ["Treat the session ID as a password — generate, protect and expire it like one"],
    diff: "Intermediate", time: "3–4 hours",
  },
  "session-lifecycle": {
    d: "The session lifecycle is a session's full journey: creation at login, storage server- or client-side, validation on each request, idle timeout, and destruction at logout or expiry. Managing each stage is what makes sessions secure and reliable.",
    why: "Lifecycle mistakes — sessions that never expire, no logout, unlimited idle time — are common, exploitable flaws in real apps.",
    obj: ["Create sessions securely at login", "Enforce idle and absolute timeouts", "Destroy sessions on logout", "Handle rotation after privilege changes"],
    prereq: ["Sessions and cookies"],
    res: [
      res("Session lifecycle — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html#lifecycle", "reference"),
      res("Session management — Microsoft Learn", "https://learn.microsoft.com/en-us/aspnet/core/security/authentication/", "docs"),
      res("Logout and session expiry — Auth0", "https://auth0.com/docs/authenticate/login/logout", "docs"),
    ],
    int: ["When should a session expire?", "Why rotate session IDs on login?", "What is the difference between idle and absolute timeout?"],
    tips: ["Always invalidate the server-side session on logout, not just the cookie"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "cookie-attributes-httponly-secure": {
    d: "Cookie attributes (HttpOnly, Secure, SameSite) are the flags that harden cookies: HttpOnly blocks JavaScript access (stopping XSS theft), Secure requires HTTPS, and SameSite limits cross-site sending (blocking CSRF).",
    why: "These three attributes prevent two of the biggest cookie threats — XSS-based theft and CSRF — with a few lines of configuration.",
    obj: ["Set HttpOnly, Secure and SameSite on session cookies", "Explain what each attribute defends against", "Choose the right SameSite value", "Audit cookies in the browser"],
    prereq: ["Sessions and cookies", "XSS and CSRF basics"],
    res: [
      res("Set-Cookie — MDN Web Docs", "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie", "docs"),
      res("SameSite cookies — web.dev", "https://web.dev/articles/samesite-cookies-explained", "article"),
      res("Cookie security — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html#cookie-attributes", "reference"),
    ],
    int: ["What does HttpOnly protect against?", "How does SameSite=Lax stop CSRF?", "Why must Secure cookies be HTTPS-only?"],
    tips: ["Default every session cookie to HttpOnly, Secure, SameSite=Lax"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "session-storage-and-revocation": {
    d: "Session storage and revocation is where sessions live (server-side stores, signed cookies, Redis) and how they are killed: revocation means invalidating sessions on logout, password change or compromise — often via a revocation list or version counter.",
    why: "If you can't revoke a session, a stolen one is a permanent backdoor — which is why revocation is a core design requirement, not an afterthought.",
    obj: ["Choose between server-side and cookie-based sessions", "Implement logout and forced revocation", "Handle revoking on password change", "Understand trade-offs of distributed session stores"],
    prereq: ["Session lifecycle", "Sessions and cookies"],
    res: [
      res("Session storage — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html#storage", "reference"),
      res("Redis sessions — Redis docs", "https://redis.io/docs/latest/develop/data-types/", "docs"),
      res("JWT revocation — Auth0", "https://auth0.com/docs/secure/tokens/json-web-tokens/", "docs"),
    ],
    int: ["How do you revoke a server-side session?", "Why is revoking a JWT hard?", "Where should sessions live in a multi-server app?"],
    tips: ["Design revocation into the session model before you scale to multiple servers"],
    diff: "Advanced", time: "3–4 hours",
  },
  "jwt-vs-sessions": {
    d: "JWT vs sessions is the authentication storage trade-off: sessions keep state server-side with an opaque ID cookie (easy to revoke, needs storage), JWTs are self-contained signed tokens (stateless, hard to revoke, size grows).",
    why: "Choosing between them is a real architecture decision — stateless APIs and microservices commonly reach for JWTs, web apps for sessions.",
    obj: ["Explain how each works end to end", "Compare revocation, storage and scaling trade-offs", "Know when each is the right fit", "Spot common JWT misuse"],
    prereq: ["Sessions and cookies", "Authentication"],
    res: [
      res("Cookies vs tokens — Auth0", "https://auth0.com/blog/cookies-vs-tokens-definitive-guide/", "article"),
      res("JWTs explained — jwt.io", "https://jwt.io/introduction", "article"),
      res("Sessions vs JWTs — freeCodeCamp", "https://www.freecodecamp.org/news/sessions-vs-jwts/", "article"),
    ],
    int: ["When would you choose sessions over JWTs?", "Why are JWTs hard to revoke?", "What is the problem with storing sensitive data in a JWT?"],
    tips: ["If you need revocation, sessions are simpler; if you need statelessness, JWTs — know the trade-off"],
    diff: "Advanced", time: "3–4 hours",
  },
  "logout-and-expiry": {
    d: "Logout and expiry are the end-of-life controls for a session: logout destroys the session server-side and clears the cookie, while expiry limits how long a session can live through idle and absolute timeouts.",
    why: "Forgotten sessions are an open door — logout and expiry are cheap controls that close it, and their absence is a common finding in security reviews.",
    obj: ["Implement a real logout that invalidates the session", "Set idle and absolute expiration", "Clear cookies client-side", "Handle expiry gracefully in the UI"],
    prereq: ["Session lifecycle", "Sessions and cookies"],
    res: [
      res("Logout — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html#expiration", "reference"),
      res("Session timeouts — Microsoft Learn", "https://learn.microsoft.com/en-us/aspnet/core/security/authentication/", "docs"),
      res("Logout best practices — Auth0", "https://auth0.com/docs/authenticate/login/logout", "docs"),
    ],
    int: ["Why is client-side-only logout insufficient?", "What happens when a session expires mid-request?", "How short should idle timeouts be?"],
    tips: ["Logout is a server-side operation — the cookie is just the reminder"],
    diff: "Intermediate", time: "2 hours",
  },
  "reporting-findings": {
    d: "Reporting findings is turning test results into clear, actionable vulnerability reports: severity, evidence, reproduction steps, impact and remediation — written so developers can fix and managers can prioritize.",
    why: "A pentest or security review is only as good as its report; clear reporting is what actually gets vulnerabilities fixed.",
    obj: ["Write severity-rated findings with evidence", "Include reproduction steps and impact", "Recommend concrete remediation", "Present findings to technical and non-technical audiences"],
    prereq: ["Vulnerability knowledge", "Report writing"],
    res: [
      res("Writing findings — PortSwigger", "https://portswigger.net/burp/documentation/desktop/tools/target/site-map", "docs"),
      res("Vulnerability disclosure — OWASP", "https://owasp.org/www-community/vulnerability_disclosure", "docs"),
      res("CVSS scoring guide — FIRST", "https://www.first.org/cvss/v3.1/specification-document", "docs"),
    ],
    int: ["What makes a finding actionable?", "How do you assign severity?", "How do you report to non-technical stakeholders?"],
    tips: ["Write findings so a developer can reproduce and fix without asking questions"],
    diff: "Advanced", time: "3–4 hours",
  },

  // ── Observability ──────────────────────────────────────────────────────────
  "logging-structured-logs": {
    d: "Logging and structured logs are recording application events in a machine-parseable format — JSON key-value fields instead of free text — so logs can be searched, filtered and aggregated across a fleet of services.",
    why: "Logs are the first place you look when something breaks, and structured logs are what make 'grep a file' scale to 'query a billion events'.",
    obj: ["Log in structured key-value format", "Include context (request ID, user, service)", "Choose appropriate log levels", "Avoid logging secrets and PII"],
    prereq: ["Basic application development"],
    res: [
      res("Structured logging — Microsoft Learn", "https://learn.microsoft.com/en-us/dotnet/core/extensions/logging", "docs"),
      res("Logging best practices — Grafana", "https://grafana.com/blog/2024/05/13/logging-best-practices/", "article"),
      res("12-factor logging — 12factor.net", "https://12factor.net/logs", "article"),
    ],
    int: ["Why are structured logs better than free text?", "What context belongs in every log line?", "What should never be logged?"],
    tips: ["Log JSON with fields — never searchable sentences"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "structured-logging-formats": {
    d: "Structured logging formats are the concrete shapes logs take — JSON, key-value pairs, logfmt, or ECS (Elastic Common Schema) — each with defined field conventions that make logs consistent and queryable across services.",
    why: "A common format is what lets one query language search every service's logs; without it, each team's logs need their own parser.",
    obj: ["Emit JSON or logfmt logs", "Adopt a shared schema like ECS", "Keep field names consistent across services", "Parse and query structured logs"],
    prereq: ["Logging and structured logs"],
    res: [
      res("Elastic Common Schema — ECS docs", "https://www.elastic.co/guide/en/ecs/current/index.html", "docs"),
      res("logfmt spec", "https://brandur.org/logfmt", "reference"),
      res("Structured logging in Go — Go docs", "https://go.dev/blog/slog", "article"),
    ],
    int: ["Why share a log schema across services?", "What is the trade-off of JSON vs logfmt?", "How do you evolve a schema without breaking queries?"],
    tips: ["Agree on a schema before the second service starts logging"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "log-levels": {
    d: "Log levels are the severity classification of log events — DEBUG, INFO, WARN, ERROR, FATAL — that let you filter noise and escalate problems. Choosing the right level is what keeps logs useful in production.",
    why: "Bad leveling produces either unsearchable noise (everything at INFO) or blind spots (everything at DEBUG that never ships) — the skill is leveling deliberately.",
    obj: ["Use levels consistently (DEBUG/INFO/WARN/ERROR)", "Emit actionable ERROR logs with context", "Control verbosity by environment", "Filter and alert on ERROR+ logs"],
    prereq: ["Logging and structured logs"],
    res: [
      res("Log levels — Microsoft Learn", "https://learn.microsoft.com/en-us/dotnet/core/extensions/logging#log-level", "docs"),
      res("Logging levels — Grafana", "https://grafana.com/blog/2024/01/22/how-to-get-the-most-out-of-loki/", "article"),
      res("Python logging — Real Python", "https://realpython.com/python-logging/", "tutorial"),
    ],
    int: ["When is a WARN vs an ERROR?", "Why should production run at INFO, not DEBUG?", "What makes an error log actionable?"],
    tips: ["ERROR should mean 'someone must look' — include what and why"],
    diff: "Beginner", time: "1–2 hours",
  },
  "centralized-logging": {
    d: "Centralized logging aggregates logs from all servers and services into one searchable system — via agents, collectors and a log store like Loki, Elasticsearch or CloudWatch — so you can query the whole fleet from one place.",
    why: "With many instances, logs live everywhere; centralization is what turns 'SSH into each box and grep' into one query across everything.",
    obj: ["Ship logs from apps and hosts to a central store", "Collect with agents (Promtail, Filebeat)", "Query across services and time ranges", "Retain and manage log storage costs"],
    prereq: ["Logging basics", "Basic infrastructure"],
    res: [
      res("Loki — Grafana docs", "https://grafana.com/docs/loki/latest/", "docs"),
      res("Elasticsearch — Elastic docs", "https://www.elastic.co/guide/en/elasticsearch/reference/current/index.html", "docs"),
      res("CloudWatch Logs — AWS docs", "https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/WhatIsCloudWatchLogs.html", "docs"),
    ],
    int: ["Why centralize logs at all?", "How do agents collect logs?", "How do you control log storage cost?"],
    tips: ["Standardize on one aggregator early — migrating later is painful"],
    diff: "Advanced", time: "3–4 hours",
  },
  "log-search-and-retention": {
    d: "Log search and retention is querying the central log store (Loki LogQL, Elasticsearch queries, CloudWatch filters) and deciding how long logs live — balancing compliance needs, debugging value and storage cost.",
    why: "Search is how you actually use logs in an incident, and retention policy is how log storage stays affordable and compliant.",
    obj: ["Write searches across time and labels", "Filter, aggregate and extract fields from logs", "Set retention tiers by log value", "Meet compliance retention requirements"],
    prereq: ["Centralized logging"],
    res: [
      res("LogQL — Grafana Loki docs", "https://grafana.com/docs/loki/latest/query/", "docs"),
      res("Query DSL — Elastic docs", "https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl.html", "docs"),
      res("Log retention — CloudWatch docs", "https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/Working-with-log-groups-and-streams.html", "docs"),
    ],
    int: ["How do you search for one request across services?", "How do you decide a retention period?", "What is the cost of full-retention?"],
    tips: ["Keep hot logs short-retention, cold/audit logs longer — tier by value"],
    diff: "Advanced", time: "3 hours",
  },
  "correlation-ids": {
    d: "Correlation IDs are unique identifiers generated at the start of a request and propagated through every service and log line it touches, letting you trace one user request across a distributed system.",
    why: "Without correlation IDs, a request that touches five services produces five unrelated log streams — correlation IDs are what let you follow it end to end.",
    obj: ["Generate and propagate a request ID", "Include it in every log line and response header", "Carry it through queues and async work", "Use it to debug cross-service issues"],
    prereq: ["Centralized logging", "Microservices basics"],
    res: [
      res("Correlation IDs — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/best-practices/correlation-id", "article"),
      res("Distributed tracing — OpenTelemetry", "https://opentelemetry.io/docs/concepts/signals/traces/", "docs"),
      res("Request IDs in practice — Cloudflare", "https://blog.cloudflare.com/request-id/", "article"),
    ],
    int: ["How do correlation IDs help debug microservices?", "Where do you generate them?", "How do you propagate them through async work?"],
    tips: ["Emit the correlation ID in every log line and in the HTTP response header"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "metrics-prometheus": {
    d: "Metrics and Prometheus is the measurement layer of observability: Prometheus scrapes numeric time-series (counters, gauges, histograms) from instrumented services, storing them with labels for flexible querying and alerting.",
    why: "Metrics answer 'is the system healthy right now' with numbers — the fastest signal of a problem, and the foundation of most alerting.",
    obj: ["Understand counters, gauges and histograms", "Instrument a service with a client library", "Configure scrape targets and jobs", "Query metrics with PromQL"],
    prereq: ["Basic application development", "HTTP basics"],
    res: [
      res("Prometheus overview — Prometheus docs", "https://prometheus.io/docs/introduction/overview/", "docs"),
      res("Metric types — Prometheus docs", "https://prometheus.io/docs/concepts/metric_types/", "docs"),
      res("Instrumentation — Prometheus docs", "https://prometheus.io/docs/practices/instrumentation/", "docs"),
    ],
    int: ["What is the difference between a counter and a gauge?", "Why does Prometheus pull instead of push?", "What are labels for?"],
    tips: ["Start with the RED metrics: Rate, Errors, Duration"],
    diff: "Advanced", time: "3–4 hours",
  },
  "metric-types": {
    d: "Metric types are the four kinds of Prometheus time-series: counters (only increase), gauges (can go up and down), histograms (bucketed observations) and summaries — each with its own semantics and correct use.",
    why: "Using the wrong metric type produces wrong dashboards and alerts — a rate on a gauge or a counter reset mid-alert are classic traps.",
    obj: ["Choose the right type per measurement", "Compute rates on counters correctly", "Read histogram buckets", "Handle resets and labels correctly"],
    prereq: ["Metrics and Prometheus"],
    res: [
      res("Metric types — Prometheus docs", "https://prometheus.io/docs/concepts/metric_types/", "docs"),
      res("Histograms and summaries — Prometheus docs", "https://prometheus.io/docs/practices/histograms/", "docs"),
      res("Naming metrics — Prometheus docs", "https://prometheus.io/docs/practices/naming/", "docs"),
    ],
    int: ["When do you use a gauge instead of a counter?", "Why must you take the rate of a counter?", "What is a histogram good for?"],
    tips: ["Counters for counts, gauges for levels, histograms for latencies"],
    diff: "Advanced", time: "2–3 hours",
  },
  "promql-basics": {
    d: "PromQL basics are the query language for Prometheus: selecting series by name and labels, computing with functions like rate, increase, sum, avg, and building expressions for dashboards and alerts.",
    why: "PromQL is how you turn raw metric numbers into the answers on a dashboard — and how you write alert rules that fire for the right reasons.",
    obj: ["Select series with label matchers", "Use rate, increase, sum and avg", "Write range queries for dashboards", "Debug queries that return no data"],
    prereq: ["Metrics and Prometheus"],
    res: [
      res("Querying basics — Prometheus docs", "https://prometheus.io/docs/prometheus/latest/querying/basics/", "docs"),
      res("Operators — Prometheus docs", "https://prometheus.io/docs/prometheus/latest/querying/operators/", "docs"),
      res("PromQL tutorial — Grafana", "https://grafana.com/docs/grafana/latest/getting-started/", "tutorial"),
    ],
    int: ["What is the difference between a vector and a range vector?", "Why use rate instead of the raw counter?", "How do you aggregate across instances?"],
    tips: ["Practice in Prometheus's expression browser before writing dashboard queries"],
    diff: "Advanced", time: "3–4 hours",
  },
  "scraping-and-exporters": {
    d: "Scraping and exporters are how metrics get from systems into Prometheus: Prometheus scrapes HTTP endpoints exposing /metrics, and exporters (node_exporter, cAdvisor, blackbox_exporter) translate other systems' stats into that format.",
    why: "The scrape architecture is why Prometheus works across infrastructure — you monitor hosts, containers, databases and services the same way.",
    obj: ["Expose a /metrics endpoint from an app", "Deploy and configure exporters", "Configure scrape_configs with jobs", "Secure and scale scraping"],
    prereq: ["Metrics and Prometheus", "Basic Linux"],
    res: [
      res("node_exporter — Prometheus docs", "https://prometheus.io/docs/guides/node-exporter/", "docs"),
      res("Scrape configuration — Prometheus docs", "https://prometheus.io/docs/prometheus/latest/configuration/configuration/", "docs"),
      res("Writing exporters — Prometheus docs", "https://prometheus.io/docs/instrumenting/writing_exporters/", "docs"),
    ],
    int: ["What does node_exporter do?", "How do you add a new scrape target?", "Why expose /metrics as a convention?"],
    tips: ["Monitor the exporters themselves — a broken scraper is invisible until you need it"],
    diff: "Advanced", time: "3 hours",
  },
  "alerting-rules": {
    d: "Alerting rules are Prometheus expressions that watch metrics and fire when conditions hold — combined with Alertmanager for routing and notifications. A rule is a query, a duration, labels and a severity.",
    why: "Alerts are the difference between monitoring and responding: good rules catch problems before users do, without drowning on-call in noise.",
    obj: ["Write alert rules on metric expressions", "Set meaningful thresholds and durations", "Add severity and runbook labels", "Test rules against real data"],
    prereq: ["PromQL basics"],
    res: [
      res("Alerting rules — Prometheus docs", "https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/", "docs"),
      res("Alerting overview — Prometheus docs", "https://prometheus.io/docs/alerting/latest/overview/", "docs"),
      res("Alerting best practices — Google SRE", "https://sre.google/sre-book/practical-alerting/", "article"),
    ],
    int: ["What is the difference between a rule and a notification?", "How long should a condition persist before alerting?", "What makes a good alert message?"],
    tips: ["Every alert should have a runbook and a clear action"],
    diff: "Advanced", time: "3 hours",
  },
  "grafana-dashboards": {
    d: "Grafana dashboards are the visualization layer of observability: panels (graphs, tables, stats) wired to Prometheus or other data sources, organised into views that tell the operational story of a service at a glance.",
    why: "A good dashboard is how a team understands a system without reading raw metrics — and how an on-call engineer finds the problem in seconds.",
    obj: ["Create panels and choose visualizations", "Wire panels to Prometheus queries", "Organize dashboards by service and layer", "Share and version dashboards"],
    prereq: ["PromQL basics"],
    res: [
      res("Grafana basics — Grafana docs", "https://grafana.com/docs/grafana/latest/getting-started/", "docs"),
      res("Building dashboards — Grafana docs", "https://grafana.com/docs/grafana/latest/dashboards/", "docs"),
      res("Dashboard design — Grafana blog", "https://grafana.com/blog/2023/01/17/how-to-create-good-dashboards/", "article"),
    ],
    int: ["What makes a dashboard useful rather than pretty?", "How do you organise panels for an incident?", "What is a good default time range?"],
    tips: ["Design dashboards for the question 'is it healthy?' — not maximum graphs"],
    diff: "Intermediate", time: "3–4 hours",
  },
  "data-sources": {
    d: "Data sources are the backends Grafana connects to — Prometheus, Loki, CloudWatch, InfluxDB, Postgres — each with its own plugin, query editor and capabilities. Panels query data sources; dashboards combine them.",
    why: "Grafana's power is one UI over many backends — knowing how to add and query data sources is what makes it the observability front door.",
    obj: ["Add and configure data sources", "Query each source type", "Connect Prometheus and Loki together", "Manage access and permissions"],
    prereq: ["Grafana basics"],
    res: [
      res("Data sources — Grafana docs", "https://grafana.com/docs/grafana/latest/datasources/", "docs"),
      res("Prometheus data source — Grafana docs", "https://grafana.com/docs/grafana/latest/datasources/prometheus/", "docs"),
      res("Loki data source — Grafana docs", "https://grafana.com/docs/grafana/latest/datasources/loki/", "docs"),
    ],
    int: ["How do multiple data sources coexist in one dashboard?", "What is the difference between a data source and a dashboard?", "How do you secure data source access?"],
    tips: ["Name data sources clearly — teams often run several Prometheis"],
    diff: "Intermediate", time: "2 hours",
  },
  "panels-and-visualizations": {
    d: "Panels and visualizations are the building blocks of Grafana dashboards: time series graphs, stat panels, gauges, bar charts, heatmaps and logs panels — each chosen to make a specific measurement legible.",
    why: "The right visualization makes a metric's story obvious; the wrong one hides it — visualization choice is a real design skill in observability.",
    obj: ["Choose the right panel type per metric", "Configure axes, units and thresholds", "Combine query results in a panel", "Annotate panels for context"],
    prereq: ["Grafana dashboards", "PromQL basics"],
    res: [
      res("Panel types — Grafana docs", "https://grafana.com/docs/grafana/latest/panels-visualizations/", "docs"),
      res("Time series panel — Grafana docs", "https://grafana.com/docs/grafana/latest/panels-visualizations/visualizations/time-series/", "docs"),
      res("Thresholds — Grafana docs", "https://grafana.com/docs/grafana/latest/panels-visualizations/configure-thresholds/", "docs"),
    ],
    int: ["When is a stat panel better than a graph?", "How do thresholds help at a glance?", "What units should a latency panel use?"],
    tips: ["Set thresholds and units on every panel — context beats raw numbers"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "variables-and-templating": {
    d: "Variables and templating make Grafana dashboards reusable: dashboard variables (service, environment, region) feed queries and labels, so one dashboard serves many services instead of one per service.",
    why: "Templated dashboards are how teams monitor dozens of services without maintaining dozens of dashboards.",
    obj: ["Define dashboard variables", "Use variables in PromQL queries", "Build multi-service dashboards", "Drill down with linked variables"],
    prereq: ["Grafana dashboards", "PromQL basics"],
    res: [
      res("Variables — Grafana docs", "https://grafana.com/docs/grafana/latest/dashboards/variables/", "docs"),
      res("Add variables to queries — Grafana docs", "https://grafana.com/docs/grafana/latest/dashboards/variables/add-template-variables/", "docs"),
      res("Templating best practices — Grafana blog", "https://grafana.com/blog/2021/02/16/how-to-build-reusable-dashboards/", "article"),
    ],
    int: ["How do variables make dashboards reusable?", "How do you scope a query by variable?", "What are chained variables for?"],
    tips: ["Template on the label you already filter by in queries"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "dashboard-sharing": {
    d: "Dashboard sharing is getting Grafana dashboards to the people who need them: sharing links and snapshots, embedding, exporting JSON for version control, and managing viewer access and permissions.",
    why: "An observability dashboard only helps if the right people can see it — sharing and access control are the delivery half of the job.",
    obj: ["Share dashboards by link and snapshot", "Export and import dashboard JSON", "Control viewer and editor permissions", "Version dashboards in git"],
    prereq: ["Grafana dashboards"],
    res: [
      res("Share dashboards — Grafana docs", "https://grafana.com/docs/grafana/latest/dashboards/share-dashboards-panels/", "docs"),
      res("Dashboard JSON — Grafana docs", "https://grafana.com/docs/grafana/latest/dashboards/json-model/", "docs"),
      res("Permissions — Grafana docs", "https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/", "docs"),
    ],
    int: ["Why keep dashboards as JSON in git?", "What is the difference between view and edit access?", "How do snapshots differ from live dashboards?"],
    tips: ["Treat dashboard JSON as code: review, version and back it up"],
    diff: "Beginner", time: "1–2 hours",
  },
  "tracing-opentelemetry": {
    d: "Tracing with OpenTelemetry is the distributed-tracing standard: instrumenting requests to capture spans — each unit of work with timing and metadata — linked into traces that show a request's path across services.",
    why: "When a slow request touches five services, tracing is the only way to see which one is actually slow — it's the third pillar of observability.",
    obj: ["Understand spans, traces and context", "Instrument services with OpenTelemetry SDKs", "Propagate trace context across calls", "Export traces to a backend"],
    prereq: ["Correlation IDs", "Microservices basics"],
    res: [
      res("Traces — OpenTelemetry docs", "https://opentelemetry.io/docs/concepts/signals/traces/", "docs"),
      res("Getting started — OpenTelemetry docs", "https://opentelemetry.io/docs/getting-started/", "tutorial"),
      res("Distributed tracing — Google Cloud", "https://cloud.google.com/trace", "docs"),
    ],
    int: ["What is the difference between a span and a trace?", "How does context propagation work?", "Why is tracing essential for microservices?"],
    tips: ["Instrument the hot path first: HTTP handlers, DB calls, queue producers"],
    diff: "Advanced", time: "3–4 hours",
  },
  "spans-and-traces": {
    d: "Spans and traces are the core data model of distributed tracing: a span is one unit of work (an HTTP call, a DB query) with a name, duration and attributes; a trace is the tree of spans representing one end-to-end request.",
    why: "Reading a trace — its span tree, timings and errors — is the skill that turns tracing data into debugging insight.",
    obj: ["Read a trace's span tree", "Interpret span duration and status", "Spot slow and failing spans", "Add attributes for searchable context"],
    prereq: ["Tracing with OpenTelemetry"],
    res: [
      res("Spans — OpenTelemetry docs", "https://opentelemetry.io/docs/concepts/signals/traces/#spans", "docs"),
      res("Trace data model — W3C spec", "https://www.w3.org/TR/trace-context/", "docs"),
      res("Jaeger concepts — Jaeger docs", "https://www.jaegertracing.io/docs/latest/architecture/", "docs"),
    ],
    int: ["What is the relationship between spans and traces?", "What makes a good span name?", "How do you find the root cause from a trace?"],
    tips: ["Look for the span with the longest duration and the least child time — that's the bottleneck"],
    diff: "Advanced", time: "2–3 hours",
  },
  "context-propagation": {
    d: "Context propagation carries trace (and other) context from one service to the next — through HTTP headers like W3C traceparent, message queues and async boundaries — so spans from different services join into one trace.",
    why: "Without propagation, every service produces its own orphaned spans; propagation is the glue that makes distributed tracing actually distributed.",
    obj: ["Propagate context via HTTP headers", "Carry context through queues and async work", "Use the W3C traceparent standard", "Debug broken propagation"],
    prereq: ["Spans and traces", "HTTP basics"],
    res: [
      res("Context propagation — OpenTelemetry docs", "https://opentelemetry.io/docs/concepts/context-propagation/", "docs"),
      res("W3C trace context — W3C spec", "https://www.w3.org/TR/trace-context/", "docs"),
      res("Propagators — OpenTelemetry docs", "https://opentelemetry.io/docs/specs/otel/context/api-propagators/", "docs"),
    ],
    int: ["How does context cross service boundaries?", "What breaks propagation?", "Why is the header format standardised?"],
    tips: ["Test propagation through every queue and gateway — those are where it breaks"],
    diff: "Advanced", time: "2–3 hours",
  },
  "instrumentation": {
    d: "Instrumentation is adding observability to code: emitting spans, metrics and logs from the places that matter — HTTP handlers, database calls, external APIs — using auto-instrumentation agents or manual SDK calls.",
    why: "You can only debug what you measure; instrumentation is where observability is created, and its quality determines everything downstream.",
    obj: ["Add auto-instrumentation to a service", "Write manual spans and custom metrics", "Instrument the critical paths", "Keep overhead low and data useful"],
    prereq: ["Tracing and metrics basics", "Application development"],
    res: [
      res("Manual instrumentation — OpenTelemetry docs", "https://opentelemetry.io/docs/languages/js/instrumentation/", "docs"),
      res("Auto-instrumentation — OpenTelemetry docs", "https://opentelemetry.io/docs/concepts/instrumentation/", "docs"),
      res("Instrumentation practices — Prometheus docs", "https://prometheus.io/docs/practices/instrumentation/", "docs"),
    ],
    int: ["What is the difference between auto and manual instrumentation?", "Where should you always instrument?", "How do you avoid observability overhead?"],
    tips: ["Instrument the edges: request entry, DB, queues, external calls"],
    diff: "Advanced", time: "3–4 hours",
  },
  "backends-jaeger-tempo": {
    d: "Tracing backends (Jaeger, Tempo, Zipkin) are the storage and UI for traces: they ingest spans, store them, and let you search traces by service, operation and attributes and view their waterfall diagrams.",
    why: "The backend is where traces become queryable — choosing and operating one (often with sampling to control cost) is the delivery half of tracing.",
    obj: ["Run or connect a tracing backend", "Search traces by service and attribute", "Read waterfall views and span details", "Configure sampling to manage cost"],
    prereq: ["Spans and traces", "OpenTelemetry"],
    res: [
      res("Jaeger — official docs", "https://www.jaegertracing.io/docs/latest/", "docs"),
      res("Grafana Tempo — official docs", "https://grafana.com/docs/tempo/latest/", "docs"),
      res("Zipkin — official docs", "https://zipkin.io/pages/quickstart.html", "docs"),
    ],
    int: ["How do tracing backends store spans?", "Why do you need sampling at scale?", "What is the difference between Jaeger and Tempo?"],
    tips: ["Sample based on errors and high-latency first; tail sampling keeps the interesting traces"],
    diff: "Advanced", time: "2–3 hours",
  },
  "alerting-on-call": {
    d: "Alerting and on-call is the human side of monitoring: alert rules firing through on-call rotations, escalations and runbooks, designed so the right person responds to the right signal without burnout.",
    why: "An alert nobody acts on is noise, and on-call that pages constantly destroys teams — the discipline is designing the whole loop, not just the rule.",
    obj: ["Design on-call rotations and handoffs", "Set escalation paths and schedules", "Write runbooks for alert responses", "Track and improve alert quality"],
    prereq: ["Alerting rules", "Basic incident response"],
    res: [
      res("On-call — PagerDuty", "https://www.pagerduty.com/guides/on-call/", "article"),
      res("Practical alerting — Google SRE", "https://sre.google/sre-book/practical-alerting/", "article"),
      res("On-call rotations — Incident.io", "https://incident.io/blog/on-call-rotations", "article"),
    ],
    int: ["How do you reduce alert noise?", "What makes a good escalation policy?", "How do you hand off on-call cleanly?"],
    tips: ["Every alert needs a runbook; every noisy alert gets deleted or fixed"],
    diff: "Advanced", time: "3–4 hours",
  },
  "alert-design": {
    d: "Alert design is crafting alerts that are accurate, actionable and rare: right thresholds, right durations, right severity, and a clear signal that tells the on-call engineer exactly what to do.",
    why: "Poorly designed alerts cause alert fatigue — the most common reason real problems go unnoticed while the pager fires for nothing.",
    obj: ["Set thresholds that fire on real problems only", "Choose duration windows to avoid flapping", "Assign severity with a clear bar", "Write alerts with runbook links"],
    prereq: ["Alerting rules", "PromQL"],
    res: [
      res("Alerting best practices — Prometheus", "https://prometheus.io/docs/practices/alerting/", "docs"),
      res("Practical alerting — Google SRE", "https://sre.google/sre-book/practical-alerting/", "article"),
      res("Alert design patterns — Grafana", "https://grafana.com/docs/grafana/latest/alerting/", "docs"),
    ],
    int: ["What makes an alert actionable?", "How do you prevent alert fatigue?", "What is the difference between a page and a dashboard alert?"],
    tips: ["Design alerts backward: what do you want the human to do?"],
    diff: "Advanced", time: "2–3 hours",
  },
  "alertmanager-routing": {
    d: "Alertmanager routing is how Prometheus alerts become notifications: routing trees match alert labels to receivers (email, Slack, PagerDuty), with grouping, inhibition and silences to control volume.",
    why: "Routing is what makes a hundred alerts become one coherent notification to the right team — the difference between signal and spam.",
    obj: ["Configure routing trees and receivers", "Group related alerts into one notification", "Use silences and inhibitions", "Route by severity and team label"],
    prereq: ["Alerting rules"],
    res: [
      res("Alertmanager — Prometheus docs", "https://prometheus.io/docs/alerting/latest/alertmanager/", "docs"),
      res("Configuration — Alertmanager docs", "https://prometheus.io/docs/alerting/latest/configuration/", "docs"),
      res("Notifications — Alertmanager docs", "https://prometheus.io/docs/alerting/latest/notifications/", "docs"),
    ],
    int: ["How do routing trees match alerts to receivers?", "Why group alerts?", "When do you use a silence?"],
    tips: ["Route on a team label so each team owns its own alerts"],
    diff: "Advanced", time: "2–3 hours",
  },
  "on-call-rotations": {
    d: "On-call rotations are the schedules that decide who responds to alerts when: primary/secondary shifts, follow-the-sun coverage, handoffs and escalation, designed to distribute burden fairly and cover the system 24/7.",
    why: "Rotations are the structure that makes 24/7 reliability sustainable — fairness and clear escalation prevent burnout and dropped incidents.",
    obj: ["Set up primary/secondary rotations", "Define escalation paths and timeouts", "Handle handoffs and overrides", "Balance load across the team"],
    prereq: ["Alerting and on-call"],
    res: [
      res("On-call guide — PagerDuty", "https://www.pagerduty.com/guides/on-call/", "article"),
      res("Scheduling — Incident.io", "https://incident.io/blog/on-call-scheduling", "article"),
      res("On-call at Google — SRE book", "https://sre.google/sre-book/oncall/", "article"),
    ],
    int: ["How do you keep on-call fair?", "What is the purpose of a secondary?", "How do you handle the same person being woken repeatedly?"],
    tips: ["Rotate frequently and track alert load per person"],
    diff: "Intermediate", time: "2–3 hours",
  },

  // ── Cloud & DevOps ─────────────────────────────────────────────────────────
  "iaas-vs-paas-vs-saas": {
    d: "IaaS vs PaaS vs SaaS are the three cloud service models: IaaS gives raw infrastructure (VMs, storage, networks), PaaS adds managed platforms (databases, runtimes, app hosting), SaaS delivers finished applications. The line is who manages what.",
    why: "Every cloud architecture discussion starts with these — choosing a model is choosing how much you manage versus how much you buy.",
    obj: ["Define each model and what the provider manages", "Map AWS/Azure/GCP services to models", "Choose the right model per workload", "Understand the shared responsibility shift"],
    prereq: ["Cloud basics"],
    res: [
      res("Cloud service models — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-overview", "docs"),
      res("IaaS vs PaaS vs SaaS — Google Cloud", "https://cloud.google.com/learn/what-is-iaas-vs-paas-vs-saas", "article"),
      res("Types of cloud computing — AWS", "https://aws.amazon.com/types-of-cloud-computing/", "article"),
    ],
    int: ["What is the difference between IaaS, PaaS and SaaS?", "Who is responsible for what in each model?", "When would you choose PaaS over IaaS?"],
    tips: ["Ask 'who patches the OS?' — the answer tells you the model"],
    diff: "Beginner", time: "2–3 hours",
  },
  "public-vs-private-vs-hybrid-cloud": {
    d: "Public vs private vs hybrid cloud are the deployment models: public uses a provider's shared infrastructure, private runs dedicated infrastructure (on-prem or hosted), hybrid combines both — usually for data residency, latency or legacy reasons.",
    why: "Choosing the deployment model is a real architecture and compliance decision, and the vocabulary appears in every cloud interview.",
    obj: ["Define each deployment model", "Compare cost, control and compliance", "Explain when hybrid makes sense", "Describe migration paths between models"],
    prereq: ["Cloud basics"],
    res: [
      res("Cloud deployment models — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-overview", "docs"),
      res("Public vs private vs hybrid — Google Cloud", "https://cloud.google.com/learn/what-is-hybrid-cloud", "article"),
      res("Cloud computing types — AWS", "https://aws.amazon.com/types-of-cloud-computing/", "article"),
    ],
    int: ["What is the difference between public and private cloud?", "Why would a company choose hybrid?", "How does compliance drive the choice?"],
    tips: ["Think compliance and latency first — they usually decide the model"],
    diff: "Beginner", time: "2 hours",
  },
  "minimal-base-images": {
    d: "Minimal base images are container foundations built from slim or distroless variants — alpine, distroless, scratch — containing only the runtime, not a full OS, to shrink attack surface and image size.",
    why: "Every package in an image is a potential vulnerability; minimal bases reduce the attack surface and speed up pulls — a core container-security practice.",
    obj: ["Choose slim, distroless or scratch bases", "Compare size and security trade-offs", "Understand what the runtime actually needs", "Keep debugging capability when needed"],
    prereq: ["Docker basics", "Containers"],
    res: [
      res("Distroless images — Google", "https://github.com/GoogleContainerTools/distroless", "docs"),
      res("Best practices for base images — Docker", "https://docs.docker.com/develop/develop-images/dockerfile_best-practices/", "docs"),
      res("Alpine vs distroless — Snyk", "https://snyk.io/blog/choosing-the-best-base-image/", "article"),
    ],
    int: ["Why are smaller images more secure?", "What is the trade-off of distroless images?", "When should you keep a shell in the image?"],
    tips: ["Start from a minimal base and add only what the app needs"],
    diff: "Advanced", time: "2–3 hours",
  },
  "scanning-for-vulnerabilities": {
    d: "Scanning for vulnerabilities is running container and dependency scanners — Trivy, Grype, Snyk — against images and dependencies to find known CVEs, and gating builds or deploys on the results.",
    why: "Most container exploits use known, published CVEs — scanning catches them before they ship, making it one of the highest-ROI security practices.",
    obj: ["Scan images with Trivy or Grype", "Understand CVE severity and fixability", "Gate CI on scan results", "Keep the scanner and image base updated"],
    prereq: ["Containers", "CI/CD basics"],
    res: [
      res("Trivy — official docs", "https://trivy.dev/", "docs"),
      res("Grype — Anchore docs", "https://anchore.com/grype/", "docs"),
      res("Container security — OWASP", "https://owasp.org/www-project-kubernetes-top-ten/", "reference"),
    ],
    int: ["How do vulnerability scanners work?", "What is the difference between a fixable and unfixable CVE?", "How do you gate deployments on scans?"],
    tips: ["Scan in CI and block merges on critical CVEs — don't wait for runtime"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "least-privilege-containers": {
    d: "Least-privilege containers run with the minimum privileges the app needs: non-root users, read-only filesystems, dropped capabilities, no privileged mode, and seccomp/AppArmor profiles.",
    why: "Container escape exploits work by abusing excess privilege — a container that can't write or escalate is a much smaller target.",
    obj: ["Run containers as non-root users", "Drop capabilities and mount read-only", "Set resource limits", "Apply seccomp and AppArmor profiles"],
    prereq: ["Containers", "Linux basics"],
    res: [
      res("Security best practices — Docker", "https://docs.docker.com/engine/security/", "docs"),
      res("Kubernetes pod security — K8s docs", "https://kubernetes.io/docs/concepts/security/pod-security-standards/", "docs"),
      res("Container hardening — OWASP", "https://owasp.org/www-project-kubernetes-ten-top/", "reference"),
    ],
    int: ["Why run containers as non-root?", "What are Linux capabilities?", "How do read-only filesystems improve security?"],
    tips: ["Assume the container will be compromised — make it boring if it is"],
    diff: "Advanced", time: "3 hours",
  },
  "runtime-security": {
    d: "Runtime security is protecting containers and workloads while they run: detecting anomalies and attacks (Falco, cloud security monitors), network policy, and response — beyond the image-scanning and hardening that happens before deploy.",
    why: "Scans catch known issues at build; runtime security catches what gets past them — the difference between prevention and detection.",
    obj: ["Deploy runtime detection with Falco", "Apply network policies to workloads", "Detect suspicious process and file activity", "Respond to runtime alerts"],
    prereq: ["Containers", "Security fundamentals"],
    res: [
      res("Falco — official docs", "https://falco.org/docs/", "docs"),
      res("Runtime security — CNCF", "https://www.cncf.io/blog/2021/03/15/what-is-container-runtime-security/", "article"),
      res("Kubernetes security — K8s docs", "https://kubernetes.io/docs/concepts/security/", "docs"),
    ],
    int: ["What is the difference between image scanning and runtime security?", "What anomalies should runtime security detect?", "How do you respond to a runtime alert?"],
    tips: ["Start with the top 10 attack behaviours, not a rule for everything"],
    diff: "Advanced", time: "3–4 hours",
  },
  "images-and-containers": {
    d: "Images and containers are the two halves of containerisation: an image is the immutable blueprint (layered filesystem + config), a container is a running instance of that image. Build once, run anywhere.",
    why: "The image/container model is what makes applications portable and reproducible — the foundation of Docker and Kubernetes.",
    obj: ["Build images with Dockerfiles", "Run and manage containers", "Understand image layers and caching", "Publish and pull images from registries"],
    prereq: ["Command line", "Basic Linux"],
    res: [
      res("Docker overview — Docker docs", "https://docs.docker.com/get-started/", "tutorial"),
      res("Dockerfile reference — Docker docs", "https://docs.docker.com/reference/dockerfile/", "docs"),
      res("What is a container? — Google Cloud", "https://cloud.google.com/learn/what-are-containers", "article"),
    ],
    int: ["What is the difference between an image and a container?", "How do image layers work?", "Why are containers portable?"],
    tips: ["Learn the layer model — it explains caching, size and history"],
    diff: "Intermediate", time: "3–4 hours",
  },
  "volumes-and-networks": {
    d: "Volumes and networks are how Docker containers persist data and communicate: volumes mount storage outside the container's ephemeral filesystem, and Docker networks (bridge, host, overlay) connect containers by name.",
    why: "Containers are ephemeral by design — volumes are how data survives restarts, and networks are how multi-container apps talk to each other.",
    obj: ["Create and mount volumes", "Attach containers to networks", "Use service names for inter-container DNS", "Choose network drivers deliberately"],
    prereq: ["Images and containers"],
    res: [
      res("Volumes — Docker docs", "https://docs.docker.com/storage/volumes/", "docs"),
      res("Networking overview — Docker docs", "https://docs.docker.com/network/", "docs"),
      res("Container networking — Docker docs", "https://docs.docker.com/network/network-tutorial-standalone/", "tutorial"),
    ],
    int: ["Why do containers need volumes?", "How do containers discover each other on a network?", "What is the difference between bridge and host networking?"],
    tips: ["Use named volumes for data, bind mounts for dev config"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "networks-and-volumes": {
    d: "Networks and volumes are the two Docker resources beyond containers: networks provide isolated, named communication between containers, and volumes provide persistent, shareable storage — both declared in Compose or CLI flags.",
    why: "Real applications are multi-container — web, db, cache — and networks plus volumes are the plumbing that makes them work as one system.",
    obj: ["Declare networks in Compose", "Mount named volumes for persistence", "Connect containers across services", "Back up and manage volume data"],
    prereq: ["Images and containers", "Compose basics"],
    res: [
      res("Compose networks — Docker docs", "https://docs.docker.com/compose/networking/", "docs"),
      res("Compose volumes — Docker docs", "https://docs.docker.com/compose/compose-file/05-services/", "docs"),
      res("Volumes — Docker docs", "https://docs.docker.com/storage/volumes/", "docs"),
    ],
    int: ["How do networks and volumes work together in Compose?", "When is a volume the right persistence choice?", "How do services reach each other by name?"],
    tips: ["Let Compose create the network; attach services by name"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "multi-container-apps": {
    d: "Multi-container apps are applications built from several cooperating containers — web server, database, cache, queue — orchestrated together with Docker Compose or an orchestrator, each container doing one job.",
    why: "Microservice-style development is the standard architecture, and running the whole stack locally with Compose is how every modern developer works.",
    obj: ["Model a stack with multiple services", "Wire services with depends_on and healthchecks", "Share networks and volumes between services", "Run and debug the whole stack with Compose"],
    prereq: ["Images and containers", "Compose basics"],
    res: [
      res("Getting started with Compose — Docker docs", "https://docs.docker.com/compose/gettingstarted/", "tutorial"),
      res("Compose file reference — Docker docs", "https://docs.docker.com/compose/compose-file/", "docs"),
      res("Multi-container example — Docker docs", "https://docs.docker.com/compose/samples/", "reference"),
    ],
    int: ["How do you run a database and app together?", "What is the difference between depends_on and a healthcheck?", "How do you persist data in a multi-container app?"],
    tips: ["Mirror production topology in your Compose file"],
    diff: "Intermediate", time: "3–4 hours",
  },
  "workflow-files-and-syntax": {
    d: "Workflow files and syntax are the YAML structure of CI/CD: GitHub Actions workflows with name, on (triggers), jobs, steps and uses — or GitLab CI's stages and jobs. The file defines when and how automation runs.",
    why: "Reading and writing pipeline YAML is a daily skill for anyone shipping code — the file IS the automation.",
    obj: ["Write a valid workflow YAML", "Understand events, jobs and steps", "Use expressions and conditionals", "Debug invalid or failing workflows"],
    prereq: ["YAML basics", "Git"],
    res: [
      res("Workflow syntax — GitHub docs", "https://docs.github.com/en/actions/reference/workflow-syntax-for-github-actions", "docs"),
      res("CI/CD pipelines — GitLab docs", "https://docs.gitlab.com/ci/pipelines/", "docs"),
      res("YAML tutorial — Red Hat", "https://www.redhat.com/en/topics/automation/what-is-yaml", "article"),
    ],
    int: ["What is the difference between a job and a step?", "How do you trigger a workflow only on certain paths?", "How do you pass data between jobs?"],
    tips: ["Start with a minimal workflow and add one feature at a time"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "events-and-triggers": {
    d: "Events and triggers are what start CI/CD runs: push, pull_request, schedule, workflow_dispatch, or webhooks from other systems. Choosing triggers defines when automation happens — and when it accidentally doesn't.",
    why: "Trigger mistakes — workflows that run on every push or never on the production branch — are among the most common pipeline bugs.",
    obj: ["Configure push and PR triggers", "Use path and branch filters", "Schedule workflows with cron", "Trigger manually and via webhooks"],
    prereq: ["Workflow files and syntax", "Git"],
    res: [
      res("Events that trigger workflows — GitHub docs", "https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows", "docs"),
      res("Scheduled pipelines — GitLab docs", "https://docs.gitlab.com/ci/pipelines/schedules/", "docs"),
      res("Workflow triggers — Azure DevOps", "https://learn.microsoft.com/en-us/azure/devops/pipelines/yaml-schema/", "docs"),
    ],
    int: ["How do you run CI only for pull requests?", "What is the difference between push and pull_request events?", "How do you schedule a nightly build?"],
    tips: ["Filter triggers to the paths and branches that matter"],
    diff: "Intermediate", time: "2 hours",
  },
  "jobs-and-steps": {
    d: "Jobs and steps are the execution units of CI/CD: a job runs in its own environment (VM or container) with its own workspace, and contains steps — individual commands or actions — that run in sequence.",
    why: "Understanding job isolation and step ordering is what lets you build pipelines that fail fast and pass data correctly.",
    obj: ["Define jobs with appropriate runners", "Chain steps within a job", "Share artifacts and state between jobs", "Use matrix jobs for parallel testing"],
    prereq: ["Workflow files and syntax"],
    res: [
      res("Jobs — GitHub docs", "https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobs", "docs"),
      res("Steps — GitHub docs", "https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idsteps", "docs"),
      res("GitLab jobs — GitLab docs", "https://docs.gitlab.com/ci/jobs/", "docs"),
    ],
    int: ["What is the difference between a job and a step?", "Why are jobs isolated from each other?", "How do you run tests in parallel?"],
    tips: ["Keep steps small and named — failing pipelines should be readable"],
    diff: "Intermediate", time: "2 hours",
  },
  "secrets-and-environments": {
    d: "Secrets and environments in CI/CD are how pipelines get credentials safely: encrypted secrets (GitHub Secrets, GitLab CI/CD variables) injected as environment variables, and deployment environments with their own scoped secrets and approvals.",
    why: "Leaked credentials in pipelines are a top breach vector — the discipline is never inlining secrets and scoping them to the environments that need them.",
    obj: ["Store and reference encrypted secrets", "Scope secrets to environments", "Use environment protection rules", "Avoid logging or echoing secrets"],
    prereq: ["Workflow files and syntax", "Git"],
    res: [
      res("Using secrets — GitHub docs", "https://docs.github.com/en/actions/security-guides/using-secrets-in-github-actions", "docs"),
      res("Environments — GitHub docs", "https://docs.github.com/en/actions/deployment/using-environments-for-deployment", "docs"),
      res("CI/CD variables — GitLab docs", "https://docs.gitlab.com/ci/variables/", "docs"),
    ],
    int: ["How do you pass a secret to a step safely?", "What is the difference between a secret and a variable?", "How do environment protection rules work?"],
    tips: ["Mask secrets in logs and rotate them on any suspicion of exposure"],
    diff: "Advanced", time: "2–3 hours",
  },
  "actions-marketplace": {
    d: "The Actions marketplace is GitHub's library of reusable actions — checkout, setup-node, upload-artifact and thousands of community actions — that compose into workflows without writing shell scripts.",
    why: "Reusing battle-tested actions beats reinventing them, but supply-chain risk means choosing and pinning actions carefully.",
    obj: ["Find and evaluate marketplace actions", "Pin actions to commit SHAs", "Use official actions for common steps", "Understand the supply-chain risk of third-party actions"],
    prereq: ["Workflow files and syntax"],
    res: [
      res("Marketplace — GitHub", "https://github.com/marketplace?type=actions", "reference"),
      res("Using actions — GitHub docs", "https://docs.github.com/en/actions/learn-github-actions/understanding-github-actions", "docs"),
      res("Supply chain security — GitHub docs", "https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions", "docs"),
    ],
    int: ["How do you evaluate a third-party action?", "Why pin actions to a SHA?", "What is the risk of actions running arbitrary code?"],
    tips: ["Pin third-party actions to full commit SHAs, not tags"],
    diff: "Intermediate", time: "1–2 hours",
  },
  "pipelines-environments": {
    d: "Pipelines and environments are the CI/CD structure beyond a single job: pipelines chain stages (build, test, deploy), and environments (dev, staging, prod) group deployments with scoped rules, approvals and history.",
    why: "Multi-environment pipelines are how real teams ship safely — promoting builds through environments with gates is the industry-standard release model.",
    obj: ["Structure pipelines into stages", "Define environments with protection rules", "Promote artifacts between environments", "Track deployment history per environment"],
    prereq: ["CI/CD basics", "Workflow files and syntax"],
    res: [
      res("Environments — GitHub docs", "https://docs.github.com/en/actions/deployment/using-environments-for-deployment", "docs"),
      res("Pipelines — GitLab docs", "https://docs.gitlab.com/ci/pipelines/", "docs"),
      res("Environments — Azure DevOps", "https://learn.microsoft.com/en-us/azure/devops/pipelines/process/environments", "docs"),
    ],
    int: ["What is the difference between a stage and an environment?", "How do you gate a production deploy?", "Why promote the same artifact across environments?"],
    tips: ["Deploy the same artifact you tested — build once, promote many"],
    diff: "Advanced", time: "3 hours",
  },
  "deployment-triggers": {
    d: "Deployment triggers are what start a deployment: manual approval, push to a branch, merge to main, tag creation, schedule, or an upstream pipeline finishing. The trigger defines when software ships.",
    why: "The right trigger automates releases safely; the wrong one ships unreviewed code or never ships at all — trigger design is release governance.",
    obj: ["Trigger deploys on merge and tag", "Gate production with approvals", "Schedule and webhook triggers", "Trace which event caused a deploy"],
    prereq: ["CI/CD basics", "Git"],
    res: [
      res("Deploying with Actions — GitHub docs", "https://docs.github.com/en/actions/deployment/about-deployments", "docs"),
      res("Triggers — GitLab docs", "https://docs.gitlab.com/ci/pipelines/triggers/", "docs"),
      res("Release triggers — Azure DevOps", "https://learn.microsoft.com/en-us/azure/devops/pipelines/release/triggers", "docs"),
    ],
    int: ["When should a deploy be automatic vs approved?", "Why deploy on tag for releases?", "How do you trace what triggered a deploy?"],
    tips: ["Automate dev/staging, gate production with approvals"],
    diff: "Intermediate", time: "2 hours",
  },
  "automated-deployments": {
    d: "Automated deployments are releasing software without manual steps: CI builds and tests, then a pipeline pushes the artifact to the environment — making deploys repeatable, fast and less error-prone than hand-run commands.",
    why: "Automation is the core of DevOps — teams that deploy automatically ship more often with fewer errors than teams that deploy by hand.",
    obj: ["Automate build, test and deploy steps", "Deploy the tested artifact consistently", "Make deploys idempotent and repeatable", "Add verification after deploy"],
    prereq: ["CI/CD basics", "Deployment"],
    res: [
      res("Deploying with Actions — GitHub docs", "https://docs.github.com/en/actions/deployment/", "docs"),
      res("Continuous deployment — Atlassian", "https://www.atlassian.com/continuous-delivery/continuous-deployment", "article"),
      res("Deployment pipelines — Azure DevOps", "https://learn.microsoft.com/en-us/azure/devops/pipelines/", "docs"),
    ],
    int: ["What is the difference between CD and CI?", "How do you make a deploy repeatable?", "Why does automation reduce deployment risk?"],
    tips: ["A deploy that takes one command is a deploy you can run at 3am"],
    diff: "Intermediate", time: "3 hours",
  },
  "deployment-scripts": {
    d: "Deployment scripts are the concrete automation that ships code — shell, Make or pipeline steps that build the artifact, run migrations, restart services and verify health. They turn 'deploy' from a ritual into a command.",
    why: "Scripted deploys are reproducible and auditable — the same commands every time — which is the foundation of reliable releases.",
    obj: ["Write idempotent deploy scripts", "Build, migrate and restart in the right order", "Verify health after deploy", "Handle rollback in the script"],
    prereq: ["Shell scripting", "CI/CD basics"],
    res: [
      res("Deploy scripts best practices — DevOps", "https://www.atlassian.com/continuous-delivery/software-deployment", "article"),
      res("Shell scripting guide — tldp", "https://tldp.org/LDP/abs/html/", "docs"),
      res("Makefile basics — GNU manual", "https://www.gnu.org/software/make/manual/make.html", "docs"),
    ],
    int: ["Why should deploy scripts be idempotent?", "What order must deploy steps run in?", "How do you verify a deploy succeeded?"],
    tips: ["Run migrations as a separate, reversible step from the app rollout"],
    diff: "Advanced", time: "3 hours",
  },
  "zero-downtime-deploys": {
    d: "Zero-downtime deploys release new versions without interrupting service — rolling updates, blue-green swaps or canary shifts — so users never see an outage during a release.",
    why: "Users and SLAs don't tolerate downtime for routine releases; zero-downtime patterns are the standard answer to 'how do you deploy without breaking things?'",
    obj: ["Describe rolling, blue-green and canary patterns", "Keep old and new versions running during cutover", "Handle sessions and connections across versions", "Roll back instantly if needed"],
    prereq: ["Deployment", "Load balancing"],
    res: [
      res("Deployment strategies — Kubernetes docs", "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/", "docs"),
      res("Zero-downtime deployment — AWS", "https://docs.aws.amazon.com/whitepapers/latest/blue-green-deployments/welcome.html", "docs"),
      res("Deployment patterns — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/patterns/", "docs"),
    ],
    int: ["How does a rolling deploy avoid downtime?", "What is the difference between blue-green and canary?", "What breaks during zero-downtime deploys (sessions, migrations)?"],
    tips: ["Make schema migrations backward-compatible — the hardest part of zero-downtime"],
    diff: "Advanced", time: "3–4 hours",
  },
  "deployment-verification": {
    d: "Deployment verification is confirming a release actually works after it ships: health checks, smoke tests, metrics and log checks that run post-deploy to catch problems before users do.",
    why: "A deploy isn't done when the pipeline says success — it's done when the service proves healthy, and verification is what turns 'hopefully fine' into 'confirmed fine'.",
    obj: ["Run health checks and smoke tests after deploy", "Watch error rate and latency post-release", "Compare metrics before and after", "Define rollback criteria in advance"],
    prereq: ["Deployment", "Observability basics"],
    res: [
      res("Smoke tests — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/patterns/", "docs"),
      res("Deployment verification — Google SRE", "https://sre.google/sre-book/", "article"),
      res("Health checks — Kubernetes docs", "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/", "docs"),
    ],
    int: ["What should you check after a deploy?", "How long should you watch a new release?", "What is the difference between readiness and liveness?"],
    tips: ["Define 'good' metrics before the deploy, not after"],
    diff: "Advanced", time: "2–3 hours",
  },
  "blue-green-canary-deploys": {
    d: "Blue-green and canary deploys are the two progressive release patterns: blue-green runs two full environments and switches traffic, canary sends a small percentage of traffic to the new version and watches before full rollout.",
    why: "Both shrink the blast radius of a bad release — the choice between them is about infrastructure cost versus how quickly you can detect problems.",
    obj: ["Run a blue-green switch with routing", "Shift a canary percentage and watch metrics", "Choose the pattern for the situation", "Roll back by switching traffic instantly"],
    prereq: ["Zero-downtime deploys", "Load balancing"],
    res: [
      res("Blue-green deployments — AWS", "https://docs.aws.amazon.com/whitepapers/latest/blue-green-deployments/welcome.html", "docs"),
      res("Canary deployments — Google Cloud", "https://cloud.google.com/architecture/deploying-to-production-using-canary-deployments", "docs"),
      res("Deployment strategies — Kubernetes docs", "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/", "docs"),
    ],
    int: ["What is the difference between blue-green and canary?", "Why does blue-green cost more?", "How do you choose a canary percentage?"],
    tips: ["Blue-green for fast rollback, canary for risk-controlled rollout"],
    diff: "Advanced", time: "3 hours",
  },
  "blue-green-strategy": {
    d: "The blue-green strategy runs two identical environments — blue (current) and green (new) — and switches all traffic between them at once via the load balancer, making rollback a one-step traffic flip.",
    why: "Instant, whole-version rollback is the strategy's superpower — the price is running double infrastructure.",
    obj: ["Provision blue and green environments", "Switch traffic atomically at the load balancer", "Roll back by flipping traffic back", "Keep the old environment warm briefly after cutover"],
    prereq: ["Zero-downtime deploys", "Load balancing"],
    res: [
      res("Blue-green — Martin Fowler", "https://martinfowler.com/bliki/BlueGreenDeployment.html", "article"),
      res("Blue-green deployments — AWS", "https://docs.aws.amazon.com/whitepapers/latest/blue-green-deployments/welcome.html", "docs"),
      res("Blue-green with Kubernetes — Argo Rollouts", "https://argoproj.github.io/argo-rollouts/features/blue-green/", "docs"),
    ],
    int: ["How is rollback achieved in blue-green?", "What is the main cost of blue-green?", "How do you handle schema changes in blue-green?"],
    tips: ["Verify green fully before cutting traffic — rollback is fast, but avoid needing it"],
    diff: "Advanced", time: "2–3 hours",
  },
  "feature-flags": {
    d: "Feature flags are runtime switches that turn features on or off without deploying — enabling gradual rollouts, instant kill-switches and trunk-based development by decoupling release from deploy.",
    why: "Flags let you ship code safely (dark launches, canary features) and turn a bad feature off in seconds instead of rolling back a deploy.",
    obj: ["Implement flag evaluation in code", "Roll features out gradually by percentage", "Use flags as kill switches", "Manage flag lifecycle and cleanup"],
    prereq: ["Deployment", "Application development"],
    res: [
      res("Feature flags — Martin Fowler", "https://martinfowler.com/articles/feature-toggles.html", "article"),
      res("Feature flags guide — LaunchDarkly", "https://launchdarkly.com/guides/feature-flags/", "article"),
      res("Feature flags — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/architecture/patterns/feature-toggle", "docs"),
    ],
    int: ["What is the difference between a feature flag and a branch?", "How do you avoid flag debt?", "Why are flags good kill switches?"],
    tips: ["Set a removal date for every flag — dead flags are configuration rot"],
    diff: "Advanced", time: "3 hours",
  },
  "monitoring-during-rollout": {
    d: "Monitoring during rollout is watching the right metrics — error rate, latency, saturation — while a new version ramps up, comparing them against the previous version to decide promote, pause or roll back.",
    why: "The rollout is the highest-risk window of a release; watching the right signals during it is what makes progressive delivery safe.",
    obj: ["Define rollout health metrics in advance", "Compare new vs old version metrics", "Set automatic rollback triggers", "Decide promote vs abort with evidence"],
    prereq: ["Observability basics", "Deployment"],
    res: [
      res("Monitoring deployments — Google SRE", "https://sre.google/sre-book/", "article"),
      res("Canary analysis — Argo Rollouts", "https://argoproj.github.io/argo-rollouts/features/analysis/", "docs"),
      res("Deployment monitoring — Datadog", "https://www.datadoghq.com/blog/deployment-monitoring/", "article"),
    ],
    int: ["What metrics should gate a rollout?", "How do you compare versions fairly?", "When do you abort a rollout?"],
    tips: ["Automate the abort — humans are slow at 3am"],
    diff: "Advanced", time: "2–3 hours",
  },
  "stages-and-jobs": {
    d: "Stages and jobs structure GitLab CI pipelines: stages run in order (build → test → deploy) with jobs inside them running in parallel, gating progression so later stages only run if earlier ones pass.",
    why: "Stages enforce the pipeline's contract — nothing deploys untested — and understanding them is core GitLab CI literacy.",
    obj: ["Define stages in dependency order", "Parallelise jobs within a stage", "Control when jobs run (rules, only/except)", "Pass artifacts between stages"],
    prereq: ["CI/CD basics", "Git"],
    res: [
      res("Pipelines — GitLab docs", "https://docs.gitlab.com/ci/pipelines/", "docs"),
      res("Jobs — GitLab docs", "https://docs.gitlab.com/ci/jobs/", "docs"),
      res("Stages — GitLab docs", "https://docs.gitlab.com/ci/yaml/#stages", "docs"),
    ],
    int: ["What is the difference between a stage and a job?", "How do artifacts move between stages?", "How do you skip a stage on certain branches?"],
    tips: ["Keep stages few and jobs parallel — pipelines should be fast and obvious"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "environment-deployments": {
    d: "Environment deployments are the per-environment release flow: each environment (dev, staging, prod) gets the build with its own configuration, approval gates and verification — the structured path a release travels.",
    why: "A release that flows through environments with checks at each stop is how teams ship confidently; skipping stages is how production breaks.",
    obj: ["Map the promotion path across environments", "Configure environment-specific settings and secrets", "Gate each environment appropriately", "Track deployment state per environment"],
    prereq: ["CI/CD basics", "Environments"],
    res: [
      res("Environments — GitHub docs", "https://docs.github.com/en/actions/deployment/using-environments-for-deployment", "docs"),
      res("Environments — GitLab docs", "https://docs.gitlab.com/ci/environments/", "docs"),
      res("Environments — Azure DevOps", "https://learn.microsoft.com/en-us/azure/devops/pipelines/process/environments", "docs"),
    ],
    int: ["What is the difference between dev and prod deploys?", "Why promote the same artifact?", "What should differ between environments?"],
    tips: ["Only configuration and secrets should differ between environments"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "splitting-large-state": {
    d: "Splitting large state is the frontend architecture practice of breaking one big global state object into smaller, focused stores — per-feature or per-domain slices — to improve clarity, performance and testability.",
    why: "Monolithic stores become slow, tangled and hard to reason about as apps grow; split state is how large frontends stay maintainable.",
    obj: ["Identify state slices by domain and ownership", "Split stores into focused modules", "Keep cross-slice dependencies explicit", "Avoid premature splitting of small apps"],
    prereq: ["State management", "React or framework state"],
    res: [
      res("Splitting store state — Zustand docs", "https://zustand.docs.pmnd.rs/guides/slices-pattern", "article"),
      res("Redux state shape — Redux docs", "https://redux.js.org/usage/structuring-reducers/normalizing-state-shape", "docs"),
      res("State management — React docs", "https://react.dev/learn/managing-state", "docs"),
    ],
    int: ["When should you split global state?", "What makes a good state slice?", "How do you avoid premature splitting?"],
    tips: ["Split by domain and by who mutates what — not by file size"],
    diff: "Advanced", time: "3–4 hours",
  },
  "deployment-workflow": {
    d: "The deployment workflow is the end-to-end release process: commit → CI build and test → promote artifact → deploy to environments with gates → verify → monitor, with rollback ready at every step.",
    why: "A well-defined workflow makes releases boring and safe — the whole discipline of DevOps is designing and following this path.",
    obj: ["Map your release workflow end to end", "Automate every repeatable step", "Add gates and verification at the right points", "Rehearse rollback as part of the workflow"],
    prereq: ["CI/CD basics", "Deployment"],
    res: [
      res("Deployment workflow — AWS", "https://docs.aws.amazon.com/whitepapers/latest/overview-deployment-options/welcome.html", "docs"),
      res("CI/CD pipeline — Red Hat", "https://www.redhat.com/en/topics/devops/what-cicd-pipeline", "article"),
      res("Delivery pipeline — Atlassian", "https://www.atlassian.com/continuous-delivery", "article"),
    ],
    int: ["Walk me through your ideal release workflow.", "Where do humans add value in a deploy?", "How do you make releases boring?"],
    tips: ["If a step is manual and repeatable, automate it next"],
    diff: "Advanced", time: "3 hours",
  },
  "terragrunt": {
    d: "Terragrunt is a thin wrapper around Terraform that removes its pain points at scale: DRY configuration across environments, remote state management per environment, dependency orchestration and easier CLI workflows.",
    why: "Teams running Terraform across many environments adopt Terragrunt to avoid copying the same config dozens of times and to wire state and dependencies cleanly.",
    obj: ["Use Terragrunt blocks and inputs", "Share config across environments with DRY principles", "Manage per-environment remote state", "Orchestrate dependencies between modules"],
    prereq: ["Terraform", "HCL basics"],
    res: [
      res("Terragrunt docs — Gruntwork", "https://terragrunt.gruntwork.io/docs/", "docs"),
      res("Terragrunt vs Terraform — Gruntwork", "https://terragrunt.gruntwork.io/docs/getting-started/quick-start/", "tutorial"),
      res("Terragrunt tutorial — freeCodeCamp", "https://www.freecodecamp.org/news/terragrunt-vs-terraform/", "article"),
    ],
    int: ["What problem does Terragrunt solve?", "How does it manage remote state per environment?", "When should you adopt it over plain Terraform?"],
    tips: ["Adopt Terragrunt when you have multiple environments or modules — not for one-off configs"],
    diff: "Advanced", time: "3–4 hours",
  },
  "ci-integration": {
    d: "CI integration is connecting development tools to continuous integration: tests, linting and builds running automatically on every push or PR, with results reported back to the developer before merge.",
    why: "CI is the safety net of modern development — catching failures in minutes instead of at release — and integrating it well is a core engineering practice.",
    obj: ["Run tests, lint and build in CI", "Gate merges on CI status", "Report results to PRs and dashboards", "Keep CI fast and reliable"],
    prereq: ["Git", "Testing basics"],
    res: [
      res("About CI — GitHub docs", "https://docs.github.com/en/actions/automating-builds-and-tests/about-continuous-integration", "docs"),
      res("Continuous integration — Martin Fowler", "https://martinfowler.com/articles/continuousIntegration.html", "article"),
      res("CI best practices — Atlassian", "https://www.atlassian.com/continuous-delivery/continuous-integration", "article"),
    ],
    int: ["What is the difference between CI and CD?", "Why run tests in CI rather than locally?", "How do you keep CI fast?"],
    tips: ["Make the CI pipeline the source of truth for merge readiness"],
    diff: "Intermediate", time: "3 hours",
  },

  // ── AWS-specific ───────────────────────────────────────────────────────────
  "instance-profiles": {
    d: "Instance profiles are the AWS mechanism that attaches IAM roles to EC2 instances: an IAM role plus a profile that the instance assumes at launch, so applications on the instance get temporary credentials without storing any keys.",
    why: "Instance profiles are the secure, recommended way to give EC2 applications AWS permissions — replacing hard-coded access keys that leak constantly.",
    obj: ["Create an IAM role and instance profile", "Attach profiles to EC2 instances", "Grant least-privilege policies", "Get temporary credentials via the metadata service"],
    prereq: ["IAM basics", "EC2 basics"],
    res: [
      res("Instance profiles — AWS docs", "https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use_switch-role-ec2_instance-profiles.html", "docs"),
      res("IAM roles for EC2 — AWS docs", "https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/iam-roles-for-amazon-ec2.html", "docs"),
      res("Instance metadata — AWS docs", "https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-metadata.html", "docs"),
    ],
    int: ["Why use instance profiles instead of access keys on EC2?", "How do instances get temporary credentials?", "What is the least-privilege rule for roles?"],
    tips: ["Never put access keys on an instance — use a role"],
    diff: "Intermediate", time: "2–3 hours",
  },
  "assuming-roles": {
    d: "Assuming roles is how AWS identity switches permissions: a user or service calls sts:AssumeRole to obtain temporary credentials for a role with different (often cross-account) permissions — the basis of cross-account access.",
    why: "Role assumption is how AWS implements least privilege across accounts and how services delegate permissions securely.",
    obj: ["Create roles with trust policies", "Assume roles via CLI, console and SDKs", "Set up cross-account access", "Use temporary credentials safely"],
    prereq: ["IAM basics"],
    res: [
      res("Assuming roles — AWS docs", "https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use.html", "docs"),
      res("sts:AssumeRole — AWS API reference", "https://docs.aws.amazon.com/STS/latest/APIReference/API_AssumeRole.html", "docs"),
      res("Cross-account access — AWS docs", "https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies-cross-account-access.html", "docs"),
    ],
    int: ["How does a role trust policy work?", "Why use roles for cross-account access?", "How long do temporary credentials last?"],
    tips: ["Design trust policies narrowly — who can assume this role and when"],
    diff: "Advanced", time: "2–3 hours",
  },
  "routing-policies": {
    d: "Routing policies are the Route 53 DNS routing modes: simple, weighted, latency-based, failover, geolocation and geoproximity — each directing traffic differently for performance, resilience or compliance.",
    why: "Routing policies are how you steer users to the right region, split traffic for testing and fail over automatically — core to resilient architectures.",
    obj: ["Choose the right routing policy per goal", "Configure weighted and latency routing", "Set up failover routing", "Understand health checks with routing"],
    prereq: ["DNS basics", "Route 53 basics"],
    res: [
      res("Routing policies — AWS docs", "https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/routing-policy.html", "docs"),
      res("DNS failover — AWS docs", "https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/dns-failover.html", "docs"),
      res("Latency routing — AWS docs", "https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/routing-policy-latency.html", "docs"),
    ],
    int: ["When would you use weighted routing?", "How does failover routing recover from an outage?", "What is the difference between latency and geolocation routing?"],
    tips: ["Pair failover policies with health checks, or they're useless"],
    diff: "Advanced", time: "2–3 hours",
  },
  "hosted-zones": {
    d: "Hosted zones are Route 53's containers for DNS records: a public hosted zone for a domain you own (with NS records pointing to Route 53) or a private hosted zone for internal resolution inside a VPC.",
    why: "Hosted zones are where every domain's records live — understanding them is the foundation of DNS management in AWS.",
    obj: ["Create public and private hosted zones", "Add records (A, CNAME, MX, TXT)", "Delegate domains with NS records", "Use private zones for internal DNS"],
    prereq: ["DNS basics"],
    res: [
      res("Hosted zones — AWS docs", "https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/hosted-zones-working-with.html", "docs"),
      res("Private hosted zones — AWS docs", "https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/hosted-zones-private.html", "docs"),
      res("DNS records — AWS docs", "https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/rrsets-working-with.html", "docs"),
    ],
    int: ["What is the difference between public and private hosted zones?", "Why must NS records point to Route 53?", "How do you delegate a subdomain?"],
    tips: ["Update your registrar's NS records to the hosted zone's NS set after creation"],
    diff: "Intermediate", time: "2 hours",
  },
  "health-checks": {
    d: "Health checks in AWS (Route 53 and target groups) periodically probe endpoints — HTTP, HTTPS or TCP — to decide if a resource is healthy, driving DNS failover and load balancer routing to only healthy targets.",
    why: "Health checks are what make failover and auto-scaling actually work — without them, traffic goes to dead instances and users see errors.",
    obj: ["Create HTTP/TCP health checks", "Tune intervals, thresholds and timeouts", "Wire health checks to failover routing", "Wire target-group checks to load balancers"],
    prereq: ["Route 53 / load balancer basics", "HTTP basics"],
    res: [
      res("Health checks — Route 53 docs", "https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/health-checks.html", "docs"),
      res("Target group health checks — AWS docs", "https://docs.aws.amazon.com/elasticloadbalancing/latest/application/target-group-health-checks.html", "docs"),
      res("Health checks — AWS docs", "https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-health-checks.html", "docs"),
    ],
    int: ["What makes a good health-check endpoint?", "Why check a deep endpoint instead of just the root?", "What is the difference between healthy and unhealthy thresholds?"],
    tips: ["Health-check an endpoint that exercises the app, not just the server"],
    diff: "Intermediate", time: "2 hours",
  },
  "events": {
    d: "Events in AWS are the asynchronous triggers that connect services: EventBridge rules match events (schedules, service changes, custom events) and route them to targets like Lambda, SNS or Step Functions.",
    why: "Event-driven architecture decouples services — components react to what happens instead of being called directly — and EventBridge is the central bus.",
    obj: ["Create EventBridge rules and targets", "Schedule events and match service events", "Send custom application events", "Build decoupled event-driven flows"],
    prereq: ["AWS basics", "Lambda basics"],
    res: [
      res("EventBridge — AWS docs", "https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-what-is.html", "docs"),
      res("EventBridge rules — AWS docs", "https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-rules.html", "docs"),
      res("Event-driven architecture — AWS", "https://aws.amazon.com/event-driven-architecture/", "article"),
    ],
    int: ["How do EventBridge rules work?", "What is the benefit of event-driven over direct calls?", "How do you guarantee event delivery?"],
    tips: ["Model events as facts about the past — version them and keep them small"],
    diff: "Advanced", time: "3 hours",
  },
  "storage": {
    d: "Storage in cloud computing is where data lives: object storage (S3) for files and media, block storage (EBS) attached to servers, file storage (EFS) shared across servers, and databases for structured data — each with its own cost and access profile.",
    why: "Choosing the right storage class is a core architecture decision — it affects cost, performance and durability across every workload.",
    obj: ["Compare object, block and file storage", "Choose storage classes by access pattern", "Understand durability and availability", "Secure and lifecycle-manage storage"],
    prereq: ["Cloud basics"],
    res: [
      res("Cloud storage overview — AWS", "https://aws.amazon.com/products/storage/", "article"),
      res("S3 storage classes — AWS docs", "https://docs.aws.amazon.com/AmazonS3/latest/userguide/storage-class-intro.html", "docs"),
      res("Storage options — Google Cloud", "https://cloud.google.com/storage-options", "docs"),
    ],
    int: ["When do you use object vs block storage?", "How do storage classes affect cost?", "How is durability guaranteed?"],
    tips: ["Match the storage type to how data is read and written, not just where it's stored"],
    diff: "Intermediate", time: "3 hours",
  },

  // ── Kubernetes ─────────────────────────────────────────────────────────────
  "pods-and-workloads": {
    d: "Pods and workloads are the scheduling units of Kubernetes: a pod is the smallest deployable unit (one or more containers sharing network and storage), and workloads (Deployments, StatefulSets, DaemonSets, Jobs) manage pods at scale.",
    why: "Understanding pods and workload controllers is the entry point to Kubernetes — everything else manages or schedules around them.",
    obj: ["Describe pod structure and lifecycle", "Use Deployments for stateless apps", "Use StatefulSets for stateful apps", "Understand Jobs, CronJobs and DaemonSets"],
    prereq: ["Containers", "Basic cluster concepts"],
    res: [
      res("Pods — Kubernetes docs", "https://kubernetes.io/docs/concepts/workloads/pods/", "docs"),
      res("Workloads — Kubernetes docs", "https://kubernetes.io/docs/concepts/workloads/", "docs"),
      res("Deployments — Kubernetes docs", "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/", "docs"),
    ],
    int: ["What is the difference between a pod and a container?", "When do you use a StatefulSet instead of a Deployment?", "What does the kubelet do?"],
    tips: ["Think of pods as the unit of scheduling and Deployments as the unit of management"],
    diff: "Intermediate", time: "3–4 hours",
  },
  "services-and-networking": {
    d: "Services and networking in Kubernetes connect pods: a Service gives pods a stable virtual IP and DNS name (ClusterIP, NodePort, LoadBalancer), while Ingress routes external HTTP traffic to services by host and path.",
    why: "Pods are ephemeral — Services are what make them reachable and stable, and Ingress is how external traffic gets in.",
    obj: ["Create ClusterIP, NodePort and LoadBalancer services", "Use Service DNS names between pods", "Configure Ingress with paths and TLS", "Understand kube-proxy and DNS resolution"],
    prereq: ["Pods and workloads"],
    res: [
      res("Services — Kubernetes docs", "https://kubernetes.io/docs/concepts/services-networking/service/", "docs"),
      res("Ingress — Kubernetes docs", "https://kubernetes.io/docs/concepts/services-networking/ingress/", "docs"),
      res("Service types — Kubernetes docs", "https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types", "docs"),
    ],
    int: ["Why do pods need Services?", "What is the difference between ClusterIP and LoadBalancer?", "How does Ingress route traffic?"],
    tips: ["Rely on Service DNS names, not pod IPs — pods change constantly"],
    diff: "Intermediate", time: "3 hours",
  },
  "helm-and-deployments": {
    d: "Helm and deployments is packaging Kubernetes apps: Helm charts bundle manifests, values and templates into a single installable unit, letting one chart deploy an app to many environments with different values.",
    why: "Raw YAML doesn't scale across environments; Helm is the standard way teams package, version and manage Kubernetes deployments.",
    obj: ["Create and structure a Helm chart", "Use values and templating", "Install, upgrade and roll back releases", "Package and publish charts"],
    prereq: ["Kubernetes basics", "YAML"],
    res: [
      res("Helm — official docs", "https://helm.sh/docs/", "docs"),
      res("Chart template guide — Helm docs", "https://helm.sh/docs/chart_template_guide/", "docs"),
      res("Helm quickstart — Helm docs", "https://helm.sh/docs/intro/quickstart/", "tutorial"),
    ],
    int: ["What problem does Helm solve over raw YAML?", "How do values and templates work?", "How do you roll back a Helm release?"],
    tips: ["Keep environment differences in values files, not forked charts"],
    diff: "Advanced", time: "3–4 hours",
  },

  // ── Backend / auth concepts ────────────────────────────────────────────────
  "streams": {
    d: "Streams are sequences of data processed incrementally rather than all at once — Node.js streams for I/O, Redis streams for message queues, Dart async streams for events. They handle data that arrives over time without loading it wholesale.",
    why: "Streaming is how large files, real-time data and message pipelines stay memory-efficient — a fundamental pattern across backends and languages.",
    obj: ["Read and write data as streams", "Handle backpressure and errors", "Use streams for files, HTTP and pipes", "Use Redis streams or async streams for events"],
    prereq: ["Async programming basics", "I/O"],
    res: [
      res("Node.js streams — Node docs", "https://nodejs.org/api/stream.html", "docs"),
      res("Redis streams — Redis docs", "https://redis.io/docs/latest/develop/data-types/streams/", "docs"),
      res("Dart async streams — Dart docs", "https://dart.dev/libraries/async/async-await", "docs"),
    ],
    int: ["Why stream data instead of loading it?", "What is backpressure?", "When would you use a stream vs an array?"],
    tips: ["Pipe streams instead of manually reading chunks wherever possible"],
    diff: "Advanced", time: "3–4 hours",
  },
  "authentication": {
    d: "Authentication is verifying who a user is — passwords, MFA, OAuth, sessions or tokens — the first step of any secured API or app, distinct from authorization (what they may do).",
    why: "Authentication is the gatekeeper of every system; getting it wrong is the most direct path to a breach, and it's a guaranteed interview topic.",
    obj: ["Implement secure password handling (hashing, salting)", "Use sessions or tokens appropriately", "Add MFA and rate limiting", "Integrate OAuth/OIDC providers"],
    prereq: ["HTTP basics", "Security fundamentals"],
    res: [
      res("Authentication — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html", "reference"),
      res("Authentication — Auth0", "https://auth0.com/intro-to-iam/what-is-authentication/", "article"),
      res("Passwords — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html", "reference"),
    ],
    int: ["What is the difference between authentication and authorization?", "How should passwords be stored?", "What is MFA and why does it matter?"],
    tips: ["Hash passwords with a slow, salted algorithm (bcrypt/argon2) — never store plaintext"],
    diff: "Advanced", time: "4–5 hours",
  },
  "permissions": {
    d: "Permissions are the authorization layer deciding what an authenticated user may do — role-based access control (RBAC), policies and scopes — enforced at every layer from UI to API to database.",
    why: "Permission bugs are access-control bugs: the most impactful vulnerability class, and the design decision that shapes every feature.",
    obj: ["Model roles, permissions and policies", "Enforce authorization server-side, not just in the UI", "Apply least privilege by default", "Audit permission changes and access"],
    prereq: ["Authentication", "Access control concepts"],
    res: [
      res("Authorization — OWASP", "https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html", "reference"),
      res("RBAC — Microsoft Learn", "https://learn.microsoft.com/en-us/azure/role-based-access-control/overview", "docs"),
      res("Access control — OWASP", "https://owasp.org/www-community/Access_Control", "docs"),
    ],
    int: ["What is the difference between authentication and authorization?", "Why must authorization be enforced server-side?", "What is least privilege?"],
    tips: ["Check authorization on every endpoint, not just in the navigation"],
    diff: "Advanced", time: "3–4 hours",
  },
  "scheduling": {
    d: "Scheduling is deciding when and on what work runs: OS process scheduling (CPU time allocation), Kubernetes scheduling (which node a pod lands on) and job scheduling (cron, orchestrators). Each is about allocating limited resources over time.",
    why: "Scheduling decisions determine throughput, fairness and cost in operating systems, containers and data pipelines — a recurring systems interview topic.",
    obj: ["Explain common OS scheduling policies", "Understand how Kubernetes schedules pods", "Use cron and job schedulers for recurring work", "Reason about fairness and starvation"],
    prereq: ["Operating systems or containers basics"],
    res: [
      res("Process scheduling — GeeksforGeeks", "https://www.geeksforgeeks.org/cpu-scheduling-in-operating-systems/", "tutorial"),
      res("Kubernetes scheduling — K8s docs", "https://kubernetes.io/docs/concepts/scheduling-eviction/", "docs"),
      res("Cron — man page", "https://man7.org/linux/man-pages/man5/crontab.5.html", "docs"),
    ],
    int: ["What is the difference between preemptive and non-preemptive scheduling?", "How does the Kubernetes scheduler choose a node?", "When is cron the wrong tool?"],
    tips: ["Learn one scheduling algorithm deeply (e.g. round-robin) — the concepts transfer"],
    diff: "Advanced", time: "3 hours",
  },
};
