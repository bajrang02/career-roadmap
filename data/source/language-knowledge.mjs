// ─────────────────────────────────────────────────────────────────────────────
// Topic-specific knowledge for the core programming-language topics that the
// generic knowledge base doesn't cover. Keys are slugified labels; entries
// follow the KNOWLEDGE shape ({ d, why, obj, prereq, tips, diff, time }).
// These give genuinely specific descriptions for the most important language
// topics instead of label-aware filler.
// ─────────────────────────────────────────────────────────────────────────────

export const LANGUAGE_KNOWLEDGE = {
  // ── pointers & memory (C, C++, Go) ────────────────────────────────────────
  "pointer-basics-addresses": {
    d: "A pointer is a variable that stores the memory address of another variable instead of a value. In C, every variable lives at an address in memory, and pointers let you hold and pass those addresses around, so functions can modify the caller's data and data structures can reference each other.",
    why: "Pointers are the foundation of manual memory management, arrays, strings and dynamic data structures — and the single most important concept separating C from higher-level languages.",
    obj: ["Declare and initialize pointers", "Read an address with & and fetch a value with *", "Understand pointer types and what they point at", "Distinguish pointers from the values they reference"],
    prereq: ["Variables", "Data types", "Arrays"],
    tips: ["Always initialize pointers before use — uninitialized pointers are undefined behavior.", "Draw boxes-and-arrows diagrams when debugging pointer code."],
    diff: "Intermediate", time: "3–4 hours",
  },
  "dereferencing": {
    d: "Dereferencing a pointer means accessing the value stored at the memory address the pointer holds, using the unary * operator. It is how you read or write through a pointer to reach the underlying variable.",
    why: "Dereferencing turns an address into usable data — without it pointers would only hold meaningless numbers.",
    obj: ["Read values through pointers with *", "Write values through pointers", "Avoid dereferencing null or dangling pointers", "Understand when dereferencing is implicit (array indexing)"],
    prereq: ["Pointer basics (addresses)"],
    tips: ["A null pointer dereference crashes the program — check for NULL before use."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "pointer-arithmetic": {
    d: "Pointer arithmetic adds or subtracts integers to or from a pointer, moving it by that many elements of the pointed-to type rather than by raw bytes. p + 1 on an int* advances to the next int, which is why arrays and pointers are interchangeable in C.",
    why: "Pointer arithmetic is how C walks arrays efficiently and is the mechanism behind array indexing, iterators and low-level buffer manipulation.",
    obj: ["Increment and decrement pointers", "Subtract two pointers to find distance", "Walk arrays with pointer arithmetic", "Understand element-size scaling"],
    prereq: ["Pointer basics (addresses)", "Arrays"],
    tips: ["Pointer arithmetic on anything but an array element is undefined behavior."],
    diff: "Advanced", time: "3–4 hours",
  },
  "malloc": {
    d: "malloc (memory allocate) reserves a block of memory of a given size on the heap at runtime and returns a void pointer to it. The memory is uninitialized, so you must assign it before reading, and you own it until you call free.",
    why: "Heap allocation is how programs create data structures whose size isn't known until runtime — linked lists, dynamic arrays, strings built from user input.",
    obj: ["Allocate memory with malloc", "Cast and use the returned pointer", "Check for allocation failure (NULL)", "Pair every malloc with a free"],
    prereq: ["Pointers", "Dynamic memory"],
    tips: ["Always check malloc's return value — it returns NULL when out of memory.", "Every malloc must eventually be matched by a free."],
    diff: "Advanced", time: "2–3 hours",
  },
  "calloc": {
    d: "calloc allocates memory for an array of n elements of a given size and zero-initializes every byte before returning the pointer, unlike malloc which leaves memory uninitialized.",
    why: "Zero-initialized memory avoids the classic bug of reading garbage from freshly allocated memory and is handy for arrays of counters, flags and structs.",
    obj: ["Allocate zero-initialized arrays with calloc", "Compare calloc vs malloc trade-offs", "Free calloc'd memory"],
    prereq: ["Pointers", "malloc"],
    tips: ["calloc is slower than malloc because of the zeroing pass — use it only when zeroed memory matters."],
    diff: "Advanced", time: "1–2 hours",
  },
  "realloc": {
    d: "realloc resizes a previously allocated heap block to a new size, copying the existing contents across and returning a possibly different pointer. It either grows or shrinks the block in place when possible or moves it.",
    why: "Growing and shrinking buffers at runtime is essential for dynamic arrays, string builders and resizable data structures.",
    obj: ["Resize allocated memory with realloc", "Handle the returned pointer (it may move)", "Avoid the classic temp-variable bug", "Free the original when realloc fails"],
    prereq: ["Pointers", "malloc"],
    tips: ["Assign realloc's result to a temporary — if it fails it returns NULL and the original block is still yours."],
    diff: "Advanced", time: "1–2 hours",
  },
  "free": {
    d: "free returns a heap block previously allocated by malloc, calloc or realloc back to the system. After free, the pointer is dangling: it still holds the old address but the memory is no longer yours to use.",
    why: "Releasing memory is what prevents leaks in long-running programs; mismanaging free causes leaks, double-frees and use-after-free bugs.",
    obj: ["Free heap memory correctly", "Understand dangling pointers", "Avoid double-free and use-after-free", "Set freed pointers to NULL for safety"],
    prereq: ["malloc"],
    tips: ["After free(p), set p = NULL so a second free is a no-op instead of a crash."],
    diff: "Advanced", time: "1–2 hours",
  },
  "memory-leaks-detection": {
    d: "A memory leak happens when a program allocates heap memory and loses the pointer to it without calling free, so the block can never be released. Over time leaks exhaust memory and crash long-running programs.",
    why: "Leak detection is a core skill for systems programmers — tools like Valgrind and the LeakSanitizer make leaks visible and fixable.",
    obj: ["Recognize common leak patterns", "Use Valgrind to find leaks", "Use ASan/LSan in development", "Write RAII-style cleanup in C++"],
    prereq: ["malloc", "free"],
    tips: ["Build with address sanitizer enabled during development — it catches leaks and overflows instantly."],
    diff: "Advanced", time: "2–3 hours",
  },
  "stack-vs-heap": {
    d: "Memory in a C program is split between the stack — a fixed-size region managed automatically for function calls, local variables and return addresses — and the heap — a large region managed manually with malloc and free.",
    why: "Knowing which region a variable lives in explains lifetime, size limits and performance, and is the key to understanding pointers and leaks.",
    obj: ["Explain stack vs heap lifetime", "Know the size limits of each", "Understand why large data goes on the heap", "Avoid returning pointers to stack locals"],
    prereq: ["Variables", "Pointers", "Functions"],
    tips: ["Never return a pointer to a local variable — it dangles as soon as the function returns."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "undefined-behavior": {
    d: "Undefined behavior (UB) is any operation the C standard deliberately leaves unspecified — signed overflow, out-of-bounds array access, dereferencing null, using an uninitialized variable. The compiler may do anything at all, including appearing to work.",
    why: "UB is the source of the most dangerous C bugs because it can pass tests and fail only in production; compilers also optimize assuming UB never happens.",
    obj: ["Recognize common UB operations", "Understand why compilers assume no UB", "Use sanitizers to detect UB", "Write code that avoids UB by construction"],
    prereq: ["Pointers", "Arrays"],
    tips: ["Compile with -fsanitize=undefined during development — it pinpoints UB at runtime."],
    diff: "Advanced", time: "2–3 hours",
  },
  "storage-classes": {
    d: "Storage classes (auto, static, extern, register) control a variable's storage duration, scope and linkage in C. static gives a variable function-local persistence or file-local visibility, extern references a variable defined elsewhere, and register hints the compiler to keep a value in a CPU register.",
    why: "Storage classes determine when a variable is created, destroyed and visible across files — essential for writing multi-file C programs.",
    obj: ["Use static for persistent local state", "Declare extern to share variables across files", "Understand linkage (internal vs external)", "Explain storage duration (automatic, static, allocated)"],
    prereq: ["Variables", "Scope", "Functions"],
    tips: ["Prefer static functions in .c files to keep internal helpers out of the public API."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "header-files": {
    d: "Header files (.h) declare functions, types and macros that multiple source files share. They are included with #include and allow the compiler to type-check calls across files while each .c file keeps its implementation private.",
    why: "Headers are how C programs are organized into modules — the declaration/definition split is what makes linking and large codebases work.",
    obj: ["Write declarations in headers, definitions in .c files", "Use include guards (#ifndef)", "Understand what belongs in a header", "Share structs and typedefs across files"],
    prereq: ["Preprocessor", "Functions", "Multiple translation units"],
    tips: ["Every header needs an include guard — #pragma once or #ifndef/#define/#endif."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "preprocessor-directives": {
    d: "The C preprocessor runs before compilation and handles directives beginning with #: #include brings in files, #define creates macros, #if/#ifdef conditionally compiles code, and #pragma controls compiler behavior.",
    why: "The preprocessor controls what code actually gets compiled, making it central to portability, feature flags and code reuse in C.",
    obj: ["Use #include and understand its search paths", "Define and use macros", "Write conditional compilation with #ifdef", "Use #pragma once"],
    prereq: ["Program structure"],
    tips: ["Prefer enum and const over #define for typed constants; macros bypass type checking."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "macros-define": {
    d: "A macro (#define) is a text substitution performed by the preprocessor: every occurrence of the macro name in the source is replaced by its definition before compilation. Macros can take arguments, making them function-like but without types or runtime cost.",
    why: "Macros power configuration constants, debug logging, and performance-critical abstractions, but their lack of type safety makes them a classic source of subtle bugs.",
    obj: ["Define object-like and function-like macros", "Understand macro expansion and side effects", "Use # and ## operators", "Know when to avoid macros"],
    prereq: ["Preprocessor directives"],
    tips: ["Parenthesize every macro argument — macro arguments are not evaluated like function arguments."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "function-pointers": {
    d: "A function pointer stores the address of a function so you can call it indirectly, pass functions to other functions, and build tables of callbacks. The syntax is int (*fp)(int, int) — a pointer to a function taking two ints and returning int.",
    why: "Function pointers enable callbacks, strategy and observer patterns, and are how C achieves polymorphism without classes.",
    obj: ["Declare and assign function pointers", "Call through a pointer", "Pass functions as arguments (qsort callback)", "Build tables of function pointers"],
    prereq: ["Functions", "Pointers"],
    tips: ["Use typedef for function pointer types — the raw syntax is hard to read and easy to get wrong."],
    diff: "Advanced", time: "3–4 hours",
  },
  "bitwise-operations": {
    d: "Bitwise operators (&, |, ^, ~, <<, >>) operate on the individual bits of integers, enabling flags, masks, packing multiple values into one integer, and efficient arithmetic like multiplying by powers of two.",
    why: "Bit manipulation is essential in embedded systems, network protocols, compression, graphics and performance-critical code.",
    obj: ["Use & | ^ ~ << >> correctly", "Set, clear and toggle individual bits", "Use masks and shift idioms", "Understand signed shifts and portability"],
    prereq: ["Operators & expressions", "Data types"],
    tips: ["Use unsigned types for bit manipulation — right shift of a negative number is implementation-defined."],
    diff: "Advanced", time: "3–4 hours",
  },
  "command-line-arguments": {
    d: "Command-line arguments reach main as (int argc, char* argv[]): argc is the count of arguments and argv is an array of strings, with argv[0] being the program name and argv[1..] the user's arguments.",
    why: "Reading argv is how every CLI tool, compiler and script accepts options and file names from the shell.",
    obj: ["Read argc and argv", "Parse options and flags", "Convert strings to numbers (atoi, strtol)", "Validate argument counts"],
    prereq: ["main", "Strings", "Loops"],
    tips: ["Use strtol instead of atoi — it reports conversion errors and handles invalid input safely."],
    diff: "Intermediate", time: "2–3 hours",
  },

  // ── structs & types ────────────────────────────────────────────────────────
  "structures-struct": {
    d: "A structure (struct) is a user-defined type that groups related variables — fields of possibly different types — into one unit. structs let you model real entities like a point (x, y), a book or a record, and pass them as single values.",
    why: "Structs are the primary way C organizes related data and the building block of linked lists, trees and nearly every data structure.",
    obj: ["Declare and initialize structs", "Access fields with . and ->", "Pass structs by value and by pointer", "Nest structs inside structs"],
    prereq: ["Variables", "Data types", "Pointers"],
    tips: ["Pass large structs by pointer to avoid copying the whole structure on every call."],
    diff: "Beginner", time: "2–3 hours",
  },
  "unions": {
    d: "A union is a type where all members share the same memory location, so only one member can hold a value at a time. Its size is the size of its largest member, and writing one member overwrites the others.",
    why: "Unions save memory when only one variant of a value is needed at once and are used in type punning, variant records and low-level protocol parsing.",
    obj: ["Declare unions and understand storage sharing", "Use unions for variant data", "Compare struct vs union memory layout", "Understand type-punning risks"],
    prereq: ["Structures (struct)", "Data types"],
    tips: ["Only read the member you last wrote — reading the wrong member is undefined behavior."],
    diff: "Intermediate", time: "1–2 hours",
  },
  "enumerations-enum": {
    d: "An enumeration (enum) defines a set of named integer constants, giving readable names to a fixed set of values like days of the week or error codes. By default the constants are 0, 1, 2, … but values can be set explicitly.",
    why: "Enums replace magic numbers with self-documenting names, making code readable and less error-prone.",
    obj: ["Define and use enums", "Set explicit enum values", "Use enums in switch statements", "Compare enum vs #define constants"],
    prereq: ["Data types", "Variables"],
    tips: ["Prefer enums over #define for related constants — the compiler knows the values and can warn on unhandled cases."],
    diff: "Beginner", time: "1–2 hours",
  },
  "typedef": {
    d: "typedef creates an alias for an existing type — for example typedef unsigned long size_t_ gives a new name to unsigned long. It is used to simplify verbose types and make code self-documenting.",
    why: "Typedefs make complex declarations readable and let you change an underlying type in one place instead of across the whole program.",
    obj: ["Create type aliases with typedef", "Simplify struct and function-pointer declarations", "Understand when typedef improves clarity"],
    prereq: ["Data types", "Structures (struct)"],
    tips: ["typedef struct { … } Name; declares the struct and its alias in one step — the most common C idiom."],
    diff: "Beginner", time: "1 hour",
  },
  "const-void-pointers": {
    d: "A const pointer (const int*) points to data that must not be modified through that pointer; a void pointer (void*) is a typeless pointer that can point to any type and must be cast before use.",
    why: "const protects invariants and lets the compiler catch accidental writes; void pointers enable generic functions like memcpy and qsort that work on any type.",
    obj: ["Read const declarations correctly", "Use const to protect data", "Pass and cast void pointers", "Understand const position (pointer vs pointee)"],
    prereq: ["Pointers", "Data types"],
    tips: ["Read declarations right-to-left: const int* is pointer-to-const-int; int* const is const-pointer-to-int."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "null-pointers-safety": {
    d: "NULL (0) is the standard representation of a pointer that points at nothing. A null pointer is safe to test and compare but not to dereference — dereferencing NULL crashes the program.",
    why: "Null-pointer discipline — initialize to NULL, check before use, set to NULL after free — prevents the most common C crash.",
    obj: ["Initialize pointers to NULL", "Check pointers before dereferencing", "Set freed pointers to NULL", "Use NULL in error paths (return NULL on failure)"],
    prereq: ["Pointers", "free"],
    tips: ["Every function that returns a pointer should document and consistently use NULL for failure."],
    diff: "Intermediate", time: "1–2 hours",
  },

  // ── strings & arrays ───────────────────────────────────────────────────────
  "strings-char-arrays": {
    d: "A C string is an array of char terminated by a null byte ('\\0'). The standard library operates on this convention: functions like strlen, strcpy and strcmp walk the array until they hit the terminator.",
    why: "Strings are everywhere in C — file paths, user input, protocol data — and the null-terminator convention is the core of how they work and how they break.",
    obj: ["Create and initialize strings", "Understand the null terminator", "Use string.h functions safely", "Avoid buffer overflows with strn functions"],
    prereq: ["Arrays", "Pointers"],
    tips: ["Always size buffers as string length + 1 for the null terminator."],
    diff: "Intermediate", time: "3–4 hours",
  },
  "array-bounds-safety": {
    d: "C does not check array bounds — accessing array[10] of an int[10] compiles and reads or writes whatever memory happens to be there. Out-of-bounds writes are how most buffer-overflow vulnerabilities happen.",
    why: "Bounding discipline is a security and correctness requirement: bounds errors corrupt memory, crash programs and are exploited by attackers.",
    obj: ["Keep track of array lengths explicitly", "Validate indices before access", "Use fixed-size and bounds-checked alternatives", "Understand how overflows become exploits"],
    prereq: ["Arrays", "Pointers"],
    tips: ["Compile with -fsanitize=address and -fstack-protector to catch bounds violations during testing."],
    diff: "Advanced", time: "2–3 hours",
  },

  // ── C++ specific ───────────────────────────────────────────────────────────
  "references-vs-pointers": {
    d: "A reference is an alias for an existing variable: after int& r = x, r and x are the same object. References cannot be re-bound and cannot be null, unlike pointers which can point anywhere and be reassigned.",
    why: "References make C++ APIs safer than pointer-based C APIs — no null checks, no * and & syntax noise — while still avoiding copies.",
    obj: ["Declare and use references", "Pass by reference to modify arguments", "Choose reference vs pointer per use case", "Understand const references"],
    prereq: ["C++ variables", "Pointers"],
    tips: ["Use references for parameters and local aliases; use pointers for optional values and rebindable handles."],
    diff: "Intermediate", time: "2–3 hours",
  },
  "move-semantics": {
    d: "Move semantics let a constructor or assignment take ownership of another object's resources without copying them, using rvalue references (T&&) and std::move. After a move, the source is left in a valid but unspecified state.",
    why: "Moves eliminate the copies that made C++ slow, which is why std::vector and std::string can return large objects efficiently and why modern C++ is competitive with C.",
    obj: ["Understand lvalues vs rvalues", "Write move constructors and move assignment", "Use std::move intentionally", "Follow the rule of five"],
    prereq: ["References", "Classes & objects", "RAII"],
    tips: ["std::move is a cast, not a function call — it just tells the compiler the value is a disposable rvalue."],
    diff: "Advanced", time: "3–4 hours",
  },
  "smart-pointers": {
    d: "Smart pointers (std::unique_ptr, std::shared_ptr, std::weak_ptr) are class templates that own heap memory and release it automatically: unique_ptr has exclusive ownership, shared_ptr uses reference counting, and weak_ptr observes without owning.",
    why: "Smart pointers implement RAII for heap memory, eliminating most manual new/delete and the leaks, double-frees and dangling pointers that come with them.",
    obj: ["Use unique_ptr for exclusive ownership", "Use shared_ptr when ownership is shared", "Break cycles with weak_ptr", "Prefer smart pointers over raw new/delete"],
    prereq: ["RAII", "Templates", "Classes & objects"],
    tips: ["Use std::make_unique and std::make_shared instead of new — they are safer and faster."],
    diff: "Advanced", time: "3–4 hours",
  },
  "raii": {
    d: "RAII (Resource Acquisition Is Initialization) binds resource lifetime to object lifetime: acquire the resource in the constructor and release it in the destructor, so the resource is freed automatically when the object goes out of scope — even on exceptions.",
    why: "RAII is the cornerstone of C++ safety — it makes leaks and resource bugs nearly impossible and is why C++ has no finally blocks.",
    obj: ["Understand the RAII idiom", "Write classes that own resources", "See RAII in std::string, std::vector, std::lock_guard", "Rely on scope exit for cleanup"],
    prereq: ["Classes & objects", "Constructors & destructors"],
    tips: ["If your class owns a resource, your destructor must release it — that's the RAII contract."],
    diff: "Advanced", time: "3–4 hours",
  },
  "templates": {
    d: "Templates let you write code that works with any type: template <typename T> T max(T a, T b) generates a version of max for every type it is instantiated with. This is how the STL works and how generic algorithms are written.",
    why: "Templates give C++ its zero-cost abstractions — generic code is compiled to specialized, fast code with no runtime overhead.",
    obj: ["Write function and class templates", "Understand template instantiation", "Use type parameters and constraints (C++20 concepts)", "Specialize templates when needed"],
    prereq: ["Functions", "Classes & objects"],
    tips: ["Templates are instantiated at compile time — errors surface per-instantiation, so test each type you support."],
    diff: "Advanced", time: "4–6 hours",
  },
  "stl-containers": {
    d: "The STL provides ready-made containers: std::vector (dynamic array), std::list (doubly linked list), std::deque, std::map/std::set (balanced trees) and std::unordered_map/std::unordered_set (hash tables), all with a uniform interface.",
    why: "Using the right STL container replaces hand-written data structures with tested, optimized code — most programs never need to implement a container again.",
    obj: ["Choose the right container per use case", "Use iterators and range-based for", "Understand complexity guarantees", "Use std::string as a container"],
    prereq: ["Templates", "Classes & objects"],
    tips: ["std::vector is the default — use it unless you have a specific reason for another container."],
    diff: "Intermediate", time: "4–6 hours",
  },

  // ── closures & functions (shared across JS/Python/etc.) ────────────────────
  closures: {
    d: "A closure is a function that retains access to variables from the scope in which it was created, even after that outer scope has finished executing. The function carries its enclosing environment with it wherever it goes.",
    why: "Closures enable private state, function factories, callbacks and the functional patterns used everywhere in modern JavaScript, Python and Go.",
    obj: ["Understand lexical scope", "Create and use closures", "Build function factories", "Use closures for private state", "Avoid closure pitfalls (loop variables)"],
    prereq: ["Functions", "Scope"],
    tips: ["Each call to a factory function creates a fresh closure — state is per-call, not shared."],
    diff: "Intermediate", time: "3–4 hours",
  },
  recursion: {
    d: "Recursion is a technique where a function calls itself to solve a smaller version of the same problem. Every recursive function needs a base case that stops the calls and a recursive case that moves toward it.",
    why: "Recursion is the natural way to express tree traversal, divide-and-conquer algorithms, and problems with self-similar structure.",
    obj: ["Write recursive functions", "Identify base and recursive cases", "Trace recursion and understand the call stack", "Compare recursion vs iteration", "Avoid stack overflow with deep recursion"],
    prereq: ["Functions", "Control flow"],
    tips: ["Always write the base case first — infinite recursion crashes with a stack overflow."],
    diff: "Intermediate", time: "3–4 hours",
  },
  "variadic-functions": {
    d: "Variadic functions accept a variable number of arguments using ... in the signature (like printf). In C they are read with the stdarg.h macros (va_start, va_arg, va_end), which requires the caller and callee to agree on how many arguments there are.",
    why: "Variadics power printf-family formatting, logging and flexible APIs, but their lack of type safety makes them error-prone.",
    obj: ["Declare variadic functions", "Read arguments with va_start/va_arg/va_end", "Understand why format strings matter", "Compare variadics vs arrays of arguments"],
    prereq: ["Functions", "Pointers"],
    tips: "Use variadic functions sparingly and always document the expected arguments — the compiler cannot check them.",
    diff: "Advanced", time: "2–3 hours",
  },
};
